// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/Toflex/directory_v2/ent/businessfeature"
	"github.com/Toflex/directory_v2/ent/predicate"
)

// BusinessFeatureQuery is the builder for querying BusinessFeature entities.
type BusinessFeatureQuery struct {
	config
	ctx        *QueryContext
	order      []businessfeature.OrderOption
	inters     []Interceptor
	predicates []predicate.BusinessFeature
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the BusinessFeatureQuery builder.
func (bfq *BusinessFeatureQuery) Where(ps ...predicate.BusinessFeature) *BusinessFeatureQuery {
	bfq.predicates = append(bfq.predicates, ps...)
	return bfq
}

// Limit the number of records to be returned by this query.
func (bfq *BusinessFeatureQuery) Limit(limit int) *BusinessFeatureQuery {
	bfq.ctx.Limit = &limit
	return bfq
}

// Offset to start from.
func (bfq *BusinessFeatureQuery) Offset(offset int) *BusinessFeatureQuery {
	bfq.ctx.Offset = &offset
	return bfq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (bfq *BusinessFeatureQuery) Unique(unique bool) *BusinessFeatureQuery {
	bfq.ctx.Unique = &unique
	return bfq
}

// Order specifies how the records should be ordered.
func (bfq *BusinessFeatureQuery) Order(o ...businessfeature.OrderOption) *BusinessFeatureQuery {
	bfq.order = append(bfq.order, o...)
	return bfq
}

// First returns the first BusinessFeature entity from the query.
// Returns a *NotFoundError when no BusinessFeature was found.
func (bfq *BusinessFeatureQuery) First(ctx context.Context) (*BusinessFeature, error) {
	nodes, err := bfq.Limit(1).All(setContextOp(ctx, bfq.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{businessfeature.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (bfq *BusinessFeatureQuery) FirstX(ctx context.Context) *BusinessFeature {
	node, err := bfq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first BusinessFeature ID from the query.
// Returns a *NotFoundError when no BusinessFeature ID was found.
func (bfq *BusinessFeatureQuery) FirstID(ctx context.Context) (id string, err error) {
	var ids []string
	if ids, err = bfq.Limit(1).IDs(setContextOp(ctx, bfq.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{businessfeature.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (bfq *BusinessFeatureQuery) FirstIDX(ctx context.Context) string {
	id, err := bfq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single BusinessFeature entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one BusinessFeature entity is found.
// Returns a *NotFoundError when no BusinessFeature entities are found.
func (bfq *BusinessFeatureQuery) Only(ctx context.Context) (*BusinessFeature, error) {
	nodes, err := bfq.Limit(2).All(setContextOp(ctx, bfq.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{businessfeature.Label}
	default:
		return nil, &NotSingularError{businessfeature.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (bfq *BusinessFeatureQuery) OnlyX(ctx context.Context) *BusinessFeature {
	node, err := bfq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only BusinessFeature ID in the query.
// Returns a *NotSingularError when more than one BusinessFeature ID is found.
// Returns a *NotFoundError when no entities are found.
func (bfq *BusinessFeatureQuery) OnlyID(ctx context.Context) (id string, err error) {
	var ids []string
	if ids, err = bfq.Limit(2).IDs(setContextOp(ctx, bfq.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{businessfeature.Label}
	default:
		err = &NotSingularError{businessfeature.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (bfq *BusinessFeatureQuery) OnlyIDX(ctx context.Context) string {
	id, err := bfq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of BusinessFeatures.
func (bfq *BusinessFeatureQuery) All(ctx context.Context) ([]*BusinessFeature, error) {
	ctx = setContextOp(ctx, bfq.ctx, ent.OpQueryAll)
	if err := bfq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*BusinessFeature, *BusinessFeatureQuery]()
	return withInterceptors[[]*BusinessFeature](ctx, bfq, qr, bfq.inters)
}

// AllX is like All, but panics if an error occurs.
func (bfq *BusinessFeatureQuery) AllX(ctx context.Context) []*BusinessFeature {
	nodes, err := bfq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of BusinessFeature IDs.
func (bfq *BusinessFeatureQuery) IDs(ctx context.Context) (ids []string, err error) {
	if bfq.ctx.Unique == nil && bfq.path != nil {
		bfq.Unique(true)
	}
	ctx = setContextOp(ctx, bfq.ctx, ent.OpQueryIDs)
	if err = bfq.Select(businessfeature.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (bfq *BusinessFeatureQuery) IDsX(ctx context.Context) []string {
	ids, err := bfq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (bfq *BusinessFeatureQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, bfq.ctx, ent.OpQueryCount)
	if err := bfq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, bfq, querierCount[*BusinessFeatureQuery](), bfq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (bfq *BusinessFeatureQuery) CountX(ctx context.Context) int {
	count, err := bfq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (bfq *BusinessFeatureQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, bfq.ctx, ent.OpQueryExist)
	switch _, err := bfq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (bfq *BusinessFeatureQuery) ExistX(ctx context.Context) bool {
	exist, err := bfq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the BusinessFeatureQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (bfq *BusinessFeatureQuery) Clone() *BusinessFeatureQuery {
	if bfq == nil {
		return nil
	}
	return &BusinessFeatureQuery{
		config:     bfq.config,
		ctx:        bfq.ctx.Clone(),
		order:      append([]businessfeature.OrderOption{}, bfq.order...),
		inters:     append([]Interceptor{}, bfq.inters...),
		predicates: append([]predicate.BusinessFeature{}, bfq.predicates...),
		// clone intermediate query.
		sql:  bfq.sql.Clone(),
		path: bfq.path,
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		CreatedAt time.Time `json:"created_at,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.BusinessFeature.Query().
//		GroupBy(businessfeature.FieldCreatedAt).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (bfq *BusinessFeatureQuery) GroupBy(field string, fields ...string) *BusinessFeatureGroupBy {
	bfq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &BusinessFeatureGroupBy{build: bfq}
	grbuild.flds = &bfq.ctx.Fields
	grbuild.label = businessfeature.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		CreatedAt time.Time `json:"created_at,omitempty"`
//	}
//
//	client.BusinessFeature.Query().
//		Select(businessfeature.FieldCreatedAt).
//		Scan(ctx, &v)
func (bfq *BusinessFeatureQuery) Select(fields ...string) *BusinessFeatureSelect {
	bfq.ctx.Fields = append(bfq.ctx.Fields, fields...)
	sbuild := &BusinessFeatureSelect{BusinessFeatureQuery: bfq}
	sbuild.label = businessfeature.Label
	sbuild.flds, sbuild.scan = &bfq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a BusinessFeatureSelect configured with the given aggregations.
func (bfq *BusinessFeatureQuery) Aggregate(fns ...AggregateFunc) *BusinessFeatureSelect {
	return bfq.Select().Aggregate(fns...)
}

func (bfq *BusinessFeatureQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range bfq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, bfq); err != nil {
				return err
			}
		}
	}
	for _, f := range bfq.ctx.Fields {
		if !businessfeature.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if bfq.path != nil {
		prev, err := bfq.path(ctx)
		if err != nil {
			return err
		}
		bfq.sql = prev
	}
	return nil
}

func (bfq *BusinessFeatureQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*BusinessFeature, error) {
	var (
		nodes = []*BusinessFeature{}
		_spec = bfq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*BusinessFeature).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &BusinessFeature{config: bfq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, bfq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (bfq *BusinessFeatureQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := bfq.querySpec()
	_spec.Node.Columns = bfq.ctx.Fields
	if len(bfq.ctx.Fields) > 0 {
		_spec.Unique = bfq.ctx.Unique != nil && *bfq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, bfq.driver, _spec)
}

func (bfq *BusinessFeatureQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(businessfeature.Table, businessfeature.Columns, sqlgraph.NewFieldSpec(businessfeature.FieldID, field.TypeString))
	_spec.From = bfq.sql
	if unique := bfq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if bfq.path != nil {
		_spec.Unique = true
	}
	if fields := bfq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, businessfeature.FieldID)
		for i := range fields {
			if fields[i] != businessfeature.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := bfq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := bfq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := bfq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := bfq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (bfq *BusinessFeatureQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(bfq.driver.Dialect())
	t1 := builder.Table(businessfeature.Table)
	columns := bfq.ctx.Fields
	if len(columns) == 0 {
		columns = businessfeature.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if bfq.sql != nil {
		selector = bfq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if bfq.ctx.Unique != nil && *bfq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range bfq.predicates {
		p(selector)
	}
	for _, p := range bfq.order {
		p(selector)
	}
	if offset := bfq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := bfq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// BusinessFeatureGroupBy is the group-by builder for BusinessFeature entities.
type BusinessFeatureGroupBy struct {
	selector
	build *BusinessFeatureQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (bfgb *BusinessFeatureGroupBy) Aggregate(fns ...AggregateFunc) *BusinessFeatureGroupBy {
	bfgb.fns = append(bfgb.fns, fns...)
	return bfgb
}

// Scan applies the selector query and scans the result into the given value.
func (bfgb *BusinessFeatureGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, bfgb.build.ctx, ent.OpQueryGroupBy)
	if err := bfgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*BusinessFeatureQuery, *BusinessFeatureGroupBy](ctx, bfgb.build, bfgb, bfgb.build.inters, v)
}

func (bfgb *BusinessFeatureGroupBy) sqlScan(ctx context.Context, root *BusinessFeatureQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(bfgb.fns))
	for _, fn := range bfgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*bfgb.flds)+len(bfgb.fns))
		for _, f := range *bfgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*bfgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := bfgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// BusinessFeatureSelect is the builder for selecting fields of BusinessFeature entities.
type BusinessFeatureSelect struct {
	*BusinessFeatureQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (bfs *BusinessFeatureSelect) Aggregate(fns ...AggregateFunc) *BusinessFeatureSelect {
	bfs.fns = append(bfs.fns, fns...)
	return bfs
}

// Scan applies the selector query and scans the result into the given value.
func (bfs *BusinessFeatureSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, bfs.ctx, ent.OpQuerySelect)
	if err := bfs.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*BusinessFeatureQuery, *BusinessFeatureSelect](ctx, bfs.BusinessFeatureQuery, bfs, bfs.inters, v)
}

func (bfs *BusinessFeatureSelect) sqlScan(ctx context.Context, root *BusinessFeatureQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(bfs.fns))
	for _, fn := range bfs.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*bfs.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := bfs.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
