// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/Toflex/directory_v2/ent/ledger"
	"github.com/Toflex/directory_v2/ent/transaction"
	"github.com/Toflex/directory_v2/ent/wallet"
)

// Ledger is the model entity for the Ledger schema.
type Ledger struct {
	config `json:"-"`
	// ID of the ent.
	ID string `json:"id,omitempty"`
	// CreatedAt holds the value of the "created_at" field.
	CreatedAt time.Time `json:"created_at,omitempty"`
	// UpdatedAt holds the value of the "updated_at" field.
	UpdatedAt time.Time `json:"updated_at,omitempty"`
	// DeletedAt holds the value of the "deleted_at" field.
	DeletedAt *time.Time `json:"deleted_at,omitempty"`
	// TransactionID holds the value of the "transaction_id" field.
	TransactionID string `json:"transaction_id,omitempty"`
	// ReversalTransactionID holds the value of the "reversal_transaction_id" field.
	ReversalTransactionID string `json:"reversal_transaction_id,omitempty"`
	// WalletID holds the value of the "wallet_id" field.
	WalletID string `json:"wallet_id,omitempty"`
	// Debit holds the value of the "debit" field.
	Debit int64 `json:"debit,omitempty"`
	// Credit holds the value of the "credit" field.
	Credit int64 `json:"credit,omitempty"`
	// CurrentBalance holds the value of the "current_balance" field.
	CurrentBalance int64 `json:"current_balance,omitempty"`
	// PreviousBalance holds the value of the "previous_balance" field.
	PreviousBalance int64 `json:"previous_balance,omitempty"`
	// IsReversal holds the value of the "is_reversal" field.
	IsReversal bool `json:"is_reversal,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the LedgerQuery when eager-loading is set.
	Edges        LedgerEdges `json:"edges"`
	selectValues sql.SelectValues
}

// LedgerEdges holds the relations/edges for other nodes in the graph.
type LedgerEdges struct {
	// Transaction holds the value of the transaction edge.
	Transaction *Transaction `json:"transaction,omitempty"`
	// Wallet holds the value of the wallet edge.
	Wallet *Wallet `json:"wallet,omitempty"`
	// ReversalTransaction holds the value of the reversal_transaction edge.
	ReversalTransaction *Transaction `json:"reversal_transaction,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [3]bool
}

// TransactionOrErr returns the Transaction value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e LedgerEdges) TransactionOrErr() (*Transaction, error) {
	if e.Transaction != nil {
		return e.Transaction, nil
	} else if e.loadedTypes[0] {
		return nil, &NotFoundError{label: transaction.Label}
	}
	return nil, &NotLoadedError{edge: "transaction"}
}

// WalletOrErr returns the Wallet value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e LedgerEdges) WalletOrErr() (*Wallet, error) {
	if e.Wallet != nil {
		return e.Wallet, nil
	} else if e.loadedTypes[1] {
		return nil, &NotFoundError{label: wallet.Label}
	}
	return nil, &NotLoadedError{edge: "wallet"}
}

// ReversalTransactionOrErr returns the ReversalTransaction value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e LedgerEdges) ReversalTransactionOrErr() (*Transaction, error) {
	if e.ReversalTransaction != nil {
		return e.ReversalTransaction, nil
	} else if e.loadedTypes[2] {
		return nil, &NotFoundError{label: transaction.Label}
	}
	return nil, &NotLoadedError{edge: "reversal_transaction"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*Ledger) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case ledger.FieldIsReversal:
			values[i] = new(sql.NullBool)
		case ledger.FieldDebit, ledger.FieldCredit, ledger.FieldCurrentBalance, ledger.FieldPreviousBalance:
			values[i] = new(sql.NullInt64)
		case ledger.FieldID, ledger.FieldTransactionID, ledger.FieldReversalTransactionID, ledger.FieldWalletID:
			values[i] = new(sql.NullString)
		case ledger.FieldCreatedAt, ledger.FieldUpdatedAt, ledger.FieldDeletedAt:
			values[i] = new(sql.NullTime)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the Ledger fields.
func (l *Ledger) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case ledger.FieldID:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field id", values[i])
			} else if value.Valid {
				l.ID = value.String
			}
		case ledger.FieldCreatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field created_at", values[i])
			} else if value.Valid {
				l.CreatedAt = value.Time
			}
		case ledger.FieldUpdatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field updated_at", values[i])
			} else if value.Valid {
				l.UpdatedAt = value.Time
			}
		case ledger.FieldDeletedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field deleted_at", values[i])
			} else if value.Valid {
				l.DeletedAt = new(time.Time)
				*l.DeletedAt = value.Time
			}
		case ledger.FieldTransactionID:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field transaction_id", values[i])
			} else if value.Valid {
				l.TransactionID = value.String
			}
		case ledger.FieldReversalTransactionID:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field reversal_transaction_id", values[i])
			} else if value.Valid {
				l.ReversalTransactionID = value.String
			}
		case ledger.FieldWalletID:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field wallet_id", values[i])
			} else if value.Valid {
				l.WalletID = value.String
			}
		case ledger.FieldDebit:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field debit", values[i])
			} else if value.Valid {
				l.Debit = value.Int64
			}
		case ledger.FieldCredit:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field credit", values[i])
			} else if value.Valid {
				l.Credit = value.Int64
			}
		case ledger.FieldCurrentBalance:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field current_balance", values[i])
			} else if value.Valid {
				l.CurrentBalance = value.Int64
			}
		case ledger.FieldPreviousBalance:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field previous_balance", values[i])
			} else if value.Valid {
				l.PreviousBalance = value.Int64
			}
		case ledger.FieldIsReversal:
			if value, ok := values[i].(*sql.NullBool); !ok {
				return fmt.Errorf("unexpected type %T for field is_reversal", values[i])
			} else if value.Valid {
				l.IsReversal = value.Bool
			}
		default:
			l.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the Ledger.
// This includes values selected through modifiers, order, etc.
func (l *Ledger) Value(name string) (ent.Value, error) {
	return l.selectValues.Get(name)
}

// QueryTransaction queries the "transaction" edge of the Ledger entity.
func (l *Ledger) QueryTransaction() *TransactionQuery {
	return NewLedgerClient(l.config).QueryTransaction(l)
}

// QueryWallet queries the "wallet" edge of the Ledger entity.
func (l *Ledger) QueryWallet() *WalletQuery {
	return NewLedgerClient(l.config).QueryWallet(l)
}

// QueryReversalTransaction queries the "reversal_transaction" edge of the Ledger entity.
func (l *Ledger) QueryReversalTransaction() *TransactionQuery {
	return NewLedgerClient(l.config).QueryReversalTransaction(l)
}

// Update returns a builder for updating this Ledger.
// Note that you need to call Ledger.Unwrap() before calling this method if this Ledger
// was returned from a transaction, and the transaction was committed or rolled back.
func (l *Ledger) Update() *LedgerUpdateOne {
	return NewLedgerClient(l.config).UpdateOne(l)
}

// Unwrap unwraps the Ledger entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (l *Ledger) Unwrap() *Ledger {
	_tx, ok := l.config.driver.(*txDriver)
	if !ok {
		panic("ent: Ledger is not a transactional entity")
	}
	l.config.driver = _tx.drv
	return l
}

// String implements the fmt.Stringer.
func (l *Ledger) String() string {
	var builder strings.Builder
	builder.WriteString("Ledger(")
	builder.WriteString(fmt.Sprintf("id=%v, ", l.ID))
	builder.WriteString("created_at=")
	builder.WriteString(l.CreatedAt.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("updated_at=")
	builder.WriteString(l.UpdatedAt.Format(time.ANSIC))
	builder.WriteString(", ")
	if v := l.DeletedAt; v != nil {
		builder.WriteString("deleted_at=")
		builder.WriteString(v.Format(time.ANSIC))
	}
	builder.WriteString(", ")
	builder.WriteString("transaction_id=")
	builder.WriteString(l.TransactionID)
	builder.WriteString(", ")
	builder.WriteString("reversal_transaction_id=")
	builder.WriteString(l.ReversalTransactionID)
	builder.WriteString(", ")
	builder.WriteString("wallet_id=")
	builder.WriteString(l.WalletID)
	builder.WriteString(", ")
	builder.WriteString("debit=")
	builder.WriteString(fmt.Sprintf("%v", l.Debit))
	builder.WriteString(", ")
	builder.WriteString("credit=")
	builder.WriteString(fmt.Sprintf("%v", l.Credit))
	builder.WriteString(", ")
	builder.WriteString("current_balance=")
	builder.WriteString(fmt.Sprintf("%v", l.CurrentBalance))
	builder.WriteString(", ")
	builder.WriteString("previous_balance=")
	builder.WriteString(fmt.Sprintf("%v", l.PreviousBalance))
	builder.WriteString(", ")
	builder.WriteString("is_reversal=")
	builder.WriteString(fmt.Sprintf("%v", l.IsReversal))
	builder.WriteByte(')')
	return builder.String()
}

// Ledgers is a parsable slice of Ledger.
type Ledgers []*Ledger
