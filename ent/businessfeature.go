// Code generated by ent, DO NOT EDIT.

package ent

import (
	"encoding/json"
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/Toflex/directory_v2/ent/businessfeature"
	"github.com/Toflex/directory_v2/ent/schema"
)

// BusinessFeature is the model entity for the BusinessFeature schema.
type BusinessFeature struct {
	config `json:"-"`
	// ID of the ent.
	ID string `json:"id,omitempty"`
	// CreatedAt holds the value of the "created_at" field.
	CreatedAt time.Time `json:"created_at,omitempty"`
	// UpdatedAt holds the value of the "updated_at" field.
	UpdatedAt time.Time `json:"updated_at,omitempty"`
	// DeletedAt holds the value of the "deleted_at" field.
	DeletedAt *time.Time `json:"deleted_at,omitempty"`
	// Name holds the value of the "name" field.
	Name string `json:"name,omitempty"`
	// Identifier holds the value of the "identifier" field.
	Identifier string `json:"identifier,omitempty"`
	// If set to true, businesses will need to subscribe to have access to this feature, else it is generally available to all business
	RequireSubscription bool `json:"require_subscription,omitempty"`
	// Active holds the value of the "active" field.
	Active bool `json:"active,omitempty"`
	// Min holds the value of the "min" field.
	Min int `json:"min,omitempty"`
	// Max holds the value of the "max" field.
	Max int `json:"max,omitempty"`
	// Fee holds the value of the "fee" field.
	Fee          *schema.Fee `json:"fee,omitempty"`
	selectValues sql.SelectValues
}

// scanValues returns the types for scanning values from sql.Rows.
func (*BusinessFeature) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case businessfeature.FieldFee:
			values[i] = new([]byte)
		case businessfeature.FieldRequireSubscription, businessfeature.FieldActive:
			values[i] = new(sql.NullBool)
		case businessfeature.FieldMin, businessfeature.FieldMax:
			values[i] = new(sql.NullInt64)
		case businessfeature.FieldID, businessfeature.FieldName, businessfeature.FieldIdentifier:
			values[i] = new(sql.NullString)
		case businessfeature.FieldCreatedAt, businessfeature.FieldUpdatedAt, businessfeature.FieldDeletedAt:
			values[i] = new(sql.NullTime)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the BusinessFeature fields.
func (bf *BusinessFeature) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case businessfeature.FieldID:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field id", values[i])
			} else if value.Valid {
				bf.ID = value.String
			}
		case businessfeature.FieldCreatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field created_at", values[i])
			} else if value.Valid {
				bf.CreatedAt = value.Time
			}
		case businessfeature.FieldUpdatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field updated_at", values[i])
			} else if value.Valid {
				bf.UpdatedAt = value.Time
			}
		case businessfeature.FieldDeletedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field deleted_at", values[i])
			} else if value.Valid {
				bf.DeletedAt = new(time.Time)
				*bf.DeletedAt = value.Time
			}
		case businessfeature.FieldName:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field name", values[i])
			} else if value.Valid {
				bf.Name = value.String
			}
		case businessfeature.FieldIdentifier:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field identifier", values[i])
			} else if value.Valid {
				bf.Identifier = value.String
			}
		case businessfeature.FieldRequireSubscription:
			if value, ok := values[i].(*sql.NullBool); !ok {
				return fmt.Errorf("unexpected type %T for field require_subscription", values[i])
			} else if value.Valid {
				bf.RequireSubscription = value.Bool
			}
		case businessfeature.FieldActive:
			if value, ok := values[i].(*sql.NullBool); !ok {
				return fmt.Errorf("unexpected type %T for field active", values[i])
			} else if value.Valid {
				bf.Active = value.Bool
			}
		case businessfeature.FieldMin:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field min", values[i])
			} else if value.Valid {
				bf.Min = int(value.Int64)
			}
		case businessfeature.FieldMax:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field max", values[i])
			} else if value.Valid {
				bf.Max = int(value.Int64)
			}
		case businessfeature.FieldFee:
			if value, ok := values[i].(*[]byte); !ok {
				return fmt.Errorf("unexpected type %T for field fee", values[i])
			} else if value != nil && len(*value) > 0 {
				if err := json.Unmarshal(*value, &bf.Fee); err != nil {
					return fmt.Errorf("unmarshal field fee: %w", err)
				}
			}
		default:
			bf.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the BusinessFeature.
// This includes values selected through modifiers, order, etc.
func (bf *BusinessFeature) Value(name string) (ent.Value, error) {
	return bf.selectValues.Get(name)
}

// Update returns a builder for updating this BusinessFeature.
// Note that you need to call BusinessFeature.Unwrap() before calling this method if this BusinessFeature
// was returned from a transaction, and the transaction was committed or rolled back.
func (bf *BusinessFeature) Update() *BusinessFeatureUpdateOne {
	return NewBusinessFeatureClient(bf.config).UpdateOne(bf)
}

// Unwrap unwraps the BusinessFeature entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (bf *BusinessFeature) Unwrap() *BusinessFeature {
	_tx, ok := bf.config.driver.(*txDriver)
	if !ok {
		panic("ent: BusinessFeature is not a transactional entity")
	}
	bf.config.driver = _tx.drv
	return bf
}

// String implements the fmt.Stringer.
func (bf *BusinessFeature) String() string {
	var builder strings.Builder
	builder.WriteString("BusinessFeature(")
	builder.WriteString(fmt.Sprintf("id=%v, ", bf.ID))
	builder.WriteString("created_at=")
	builder.WriteString(bf.CreatedAt.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("updated_at=")
	builder.WriteString(bf.UpdatedAt.Format(time.ANSIC))
	builder.WriteString(", ")
	if v := bf.DeletedAt; v != nil {
		builder.WriteString("deleted_at=")
		builder.WriteString(v.Format(time.ANSIC))
	}
	builder.WriteString(", ")
	builder.WriteString("name=")
	builder.WriteString(bf.Name)
	builder.WriteString(", ")
	builder.WriteString("identifier=")
	builder.WriteString(bf.Identifier)
	builder.WriteString(", ")
	builder.WriteString("require_subscription=")
	builder.WriteString(fmt.Sprintf("%v", bf.RequireSubscription))
	builder.WriteString(", ")
	builder.WriteString("active=")
	builder.WriteString(fmt.Sprintf("%v", bf.Active))
	builder.WriteString(", ")
	builder.WriteString("min=")
	builder.WriteString(fmt.Sprintf("%v", bf.Min))
	builder.WriteString(", ")
	builder.WriteString("max=")
	builder.WriteString(fmt.Sprintf("%v", bf.Max))
	builder.WriteString(", ")
	builder.WriteString("fee=")
	builder.WriteString(fmt.Sprintf("%v", bf.Fee))
	builder.WriteByte(')')
	return builder.String()
}

// BusinessFeatures is a parsable slice of BusinessFeature.
type BusinessFeatures []*BusinessFeature
