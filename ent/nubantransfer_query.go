// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/Toflex/directory_v2/ent/nubantransfer"
	"github.com/Toflex/directory_v2/ent/predicate"
	"github.com/Toflex/directory_v2/ent/transaction"
)

// NubanTransferQuery is the builder for querying NubanTransfer entities.
type NubanTransferQuery struct {
	config
	ctx             *QueryContext
	order           []nubantransfer.OrderOption
	inters          []Interceptor
	predicates      []predicate.NubanTransfer
	withTransaction *TransactionQuery
	withFKs         bool
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the NubanTransferQuery builder.
func (ntq *NubanTransferQuery) Where(ps ...predicate.NubanTransfer) *NubanTransferQuery {
	ntq.predicates = append(ntq.predicates, ps...)
	return ntq
}

// Limit the number of records to be returned by this query.
func (ntq *NubanTransferQuery) Limit(limit int) *NubanTransferQuery {
	ntq.ctx.Limit = &limit
	return ntq
}

// Offset to start from.
func (ntq *NubanTransferQuery) Offset(offset int) *NubanTransferQuery {
	ntq.ctx.Offset = &offset
	return ntq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (ntq *NubanTransferQuery) Unique(unique bool) *NubanTransferQuery {
	ntq.ctx.Unique = &unique
	return ntq
}

// Order specifies how the records should be ordered.
func (ntq *NubanTransferQuery) Order(o ...nubantransfer.OrderOption) *NubanTransferQuery {
	ntq.order = append(ntq.order, o...)
	return ntq
}

// QueryTransaction chains the current query on the "transaction" edge.
func (ntq *NubanTransferQuery) QueryTransaction() *TransactionQuery {
	query := (&TransactionClient{config: ntq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := ntq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := ntq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(nubantransfer.Table, nubantransfer.FieldID, selector),
			sqlgraph.To(transaction.Table, transaction.FieldID),
			sqlgraph.Edge(sqlgraph.O2O, true, nubantransfer.TransactionTable, nubantransfer.TransactionColumn),
		)
		fromU = sqlgraph.SetNeighbors(ntq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first NubanTransfer entity from the query.
// Returns a *NotFoundError when no NubanTransfer was found.
func (ntq *NubanTransferQuery) First(ctx context.Context) (*NubanTransfer, error) {
	nodes, err := ntq.Limit(1).All(setContextOp(ctx, ntq.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{nubantransfer.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (ntq *NubanTransferQuery) FirstX(ctx context.Context) *NubanTransfer {
	node, err := ntq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first NubanTransfer ID from the query.
// Returns a *NotFoundError when no NubanTransfer ID was found.
func (ntq *NubanTransferQuery) FirstID(ctx context.Context) (id string, err error) {
	var ids []string
	if ids, err = ntq.Limit(1).IDs(setContextOp(ctx, ntq.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{nubantransfer.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (ntq *NubanTransferQuery) FirstIDX(ctx context.Context) string {
	id, err := ntq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single NubanTransfer entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one NubanTransfer entity is found.
// Returns a *NotFoundError when no NubanTransfer entities are found.
func (ntq *NubanTransferQuery) Only(ctx context.Context) (*NubanTransfer, error) {
	nodes, err := ntq.Limit(2).All(setContextOp(ctx, ntq.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{nubantransfer.Label}
	default:
		return nil, &NotSingularError{nubantransfer.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (ntq *NubanTransferQuery) OnlyX(ctx context.Context) *NubanTransfer {
	node, err := ntq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only NubanTransfer ID in the query.
// Returns a *NotSingularError when more than one NubanTransfer ID is found.
// Returns a *NotFoundError when no entities are found.
func (ntq *NubanTransferQuery) OnlyID(ctx context.Context) (id string, err error) {
	var ids []string
	if ids, err = ntq.Limit(2).IDs(setContextOp(ctx, ntq.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{nubantransfer.Label}
	default:
		err = &NotSingularError{nubantransfer.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (ntq *NubanTransferQuery) OnlyIDX(ctx context.Context) string {
	id, err := ntq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of NubanTransfers.
func (ntq *NubanTransferQuery) All(ctx context.Context) ([]*NubanTransfer, error) {
	ctx = setContextOp(ctx, ntq.ctx, ent.OpQueryAll)
	if err := ntq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*NubanTransfer, *NubanTransferQuery]()
	return withInterceptors[[]*NubanTransfer](ctx, ntq, qr, ntq.inters)
}

// AllX is like All, but panics if an error occurs.
func (ntq *NubanTransferQuery) AllX(ctx context.Context) []*NubanTransfer {
	nodes, err := ntq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of NubanTransfer IDs.
func (ntq *NubanTransferQuery) IDs(ctx context.Context) (ids []string, err error) {
	if ntq.ctx.Unique == nil && ntq.path != nil {
		ntq.Unique(true)
	}
	ctx = setContextOp(ctx, ntq.ctx, ent.OpQueryIDs)
	if err = ntq.Select(nubantransfer.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (ntq *NubanTransferQuery) IDsX(ctx context.Context) []string {
	ids, err := ntq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (ntq *NubanTransferQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, ntq.ctx, ent.OpQueryCount)
	if err := ntq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, ntq, querierCount[*NubanTransferQuery](), ntq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (ntq *NubanTransferQuery) CountX(ctx context.Context) int {
	count, err := ntq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (ntq *NubanTransferQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, ntq.ctx, ent.OpQueryExist)
	switch _, err := ntq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (ntq *NubanTransferQuery) ExistX(ctx context.Context) bool {
	exist, err := ntq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the NubanTransferQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (ntq *NubanTransferQuery) Clone() *NubanTransferQuery {
	if ntq == nil {
		return nil
	}
	return &NubanTransferQuery{
		config:          ntq.config,
		ctx:             ntq.ctx.Clone(),
		order:           append([]nubantransfer.OrderOption{}, ntq.order...),
		inters:          append([]Interceptor{}, ntq.inters...),
		predicates:      append([]predicate.NubanTransfer{}, ntq.predicates...),
		withTransaction: ntq.withTransaction.Clone(),
		// clone intermediate query.
		sql:  ntq.sql.Clone(),
		path: ntq.path,
	}
}

// WithTransaction tells the query-builder to eager-load the nodes that are connected to
// the "transaction" edge. The optional arguments are used to configure the query builder of the edge.
func (ntq *NubanTransferQuery) WithTransaction(opts ...func(*TransactionQuery)) *NubanTransferQuery {
	query := (&TransactionClient{config: ntq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	ntq.withTransaction = query
	return ntq
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		CreatedAt time.Time `json:"created_at,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.NubanTransfer.Query().
//		GroupBy(nubantransfer.FieldCreatedAt).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (ntq *NubanTransferQuery) GroupBy(field string, fields ...string) *NubanTransferGroupBy {
	ntq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &NubanTransferGroupBy{build: ntq}
	grbuild.flds = &ntq.ctx.Fields
	grbuild.label = nubantransfer.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		CreatedAt time.Time `json:"created_at,omitempty"`
//	}
//
//	client.NubanTransfer.Query().
//		Select(nubantransfer.FieldCreatedAt).
//		Scan(ctx, &v)
func (ntq *NubanTransferQuery) Select(fields ...string) *NubanTransferSelect {
	ntq.ctx.Fields = append(ntq.ctx.Fields, fields...)
	sbuild := &NubanTransferSelect{NubanTransferQuery: ntq}
	sbuild.label = nubantransfer.Label
	sbuild.flds, sbuild.scan = &ntq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a NubanTransferSelect configured with the given aggregations.
func (ntq *NubanTransferQuery) Aggregate(fns ...AggregateFunc) *NubanTransferSelect {
	return ntq.Select().Aggregate(fns...)
}

func (ntq *NubanTransferQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range ntq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, ntq); err != nil {
				return err
			}
		}
	}
	for _, f := range ntq.ctx.Fields {
		if !nubantransfer.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if ntq.path != nil {
		prev, err := ntq.path(ctx)
		if err != nil {
			return err
		}
		ntq.sql = prev
	}
	return nil
}

func (ntq *NubanTransferQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*NubanTransfer, error) {
	var (
		nodes       = []*NubanTransfer{}
		withFKs     = ntq.withFKs
		_spec       = ntq.querySpec()
		loadedTypes = [1]bool{
			ntq.withTransaction != nil,
		}
	)
	if ntq.withTransaction != nil {
		withFKs = true
	}
	if withFKs {
		_spec.Node.Columns = append(_spec.Node.Columns, nubantransfer.ForeignKeys...)
	}
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*NubanTransfer).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &NubanTransfer{config: ntq.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, ntq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := ntq.withTransaction; query != nil {
		if err := ntq.loadTransaction(ctx, query, nodes, nil,
			func(n *NubanTransfer, e *Transaction) { n.Edges.Transaction = e }); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (ntq *NubanTransferQuery) loadTransaction(ctx context.Context, query *TransactionQuery, nodes []*NubanTransfer, init func(*NubanTransfer), assign func(*NubanTransfer, *Transaction)) error {
	ids := make([]string, 0, len(nodes))
	nodeids := make(map[string][]*NubanTransfer)
	for i := range nodes {
		if nodes[i].transaction_nuban_transfer == nil {
			continue
		}
		fk := *nodes[i].transaction_nuban_transfer
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(transaction.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "transaction_nuban_transfer" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}

func (ntq *NubanTransferQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := ntq.querySpec()
	_spec.Node.Columns = ntq.ctx.Fields
	if len(ntq.ctx.Fields) > 0 {
		_spec.Unique = ntq.ctx.Unique != nil && *ntq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, ntq.driver, _spec)
}

func (ntq *NubanTransferQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(nubantransfer.Table, nubantransfer.Columns, sqlgraph.NewFieldSpec(nubantransfer.FieldID, field.TypeString))
	_spec.From = ntq.sql
	if unique := ntq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if ntq.path != nil {
		_spec.Unique = true
	}
	if fields := ntq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, nubantransfer.FieldID)
		for i := range fields {
			if fields[i] != nubantransfer.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := ntq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := ntq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := ntq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := ntq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (ntq *NubanTransferQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(ntq.driver.Dialect())
	t1 := builder.Table(nubantransfer.Table)
	columns := ntq.ctx.Fields
	if len(columns) == 0 {
		columns = nubantransfer.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if ntq.sql != nil {
		selector = ntq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if ntq.ctx.Unique != nil && *ntq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range ntq.predicates {
		p(selector)
	}
	for _, p := range ntq.order {
		p(selector)
	}
	if offset := ntq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := ntq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// NubanTransferGroupBy is the group-by builder for NubanTransfer entities.
type NubanTransferGroupBy struct {
	selector
	build *NubanTransferQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (ntgb *NubanTransferGroupBy) Aggregate(fns ...AggregateFunc) *NubanTransferGroupBy {
	ntgb.fns = append(ntgb.fns, fns...)
	return ntgb
}

// Scan applies the selector query and scans the result into the given value.
func (ntgb *NubanTransferGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, ntgb.build.ctx, ent.OpQueryGroupBy)
	if err := ntgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*NubanTransferQuery, *NubanTransferGroupBy](ctx, ntgb.build, ntgb, ntgb.build.inters, v)
}

func (ntgb *NubanTransferGroupBy) sqlScan(ctx context.Context, root *NubanTransferQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(ntgb.fns))
	for _, fn := range ntgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*ntgb.flds)+len(ntgb.fns))
		for _, f := range *ntgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*ntgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := ntgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// NubanTransferSelect is the builder for selecting fields of NubanTransfer entities.
type NubanTransferSelect struct {
	*NubanTransferQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (nts *NubanTransferSelect) Aggregate(fns ...AggregateFunc) *NubanTransferSelect {
	nts.fns = append(nts.fns, fns...)
	return nts
}

// Scan applies the selector query and scans the result into the given value.
func (nts *NubanTransferSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, nts.ctx, ent.OpQuerySelect)
	if err := nts.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*NubanTransferQuery, *NubanTransferSelect](ctx, nts.NubanTransferQuery, nts, nts.inters, v)
}

func (nts *NubanTransferSelect) sqlScan(ctx context.Context, root *NubanTransferQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(nts.fns))
	for _, fn := range nts.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*nts.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := nts.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
