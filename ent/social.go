// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/Toflex/directory_v2/ent/business"
	"github.com/Toflex/directory_v2/ent/social"
)

// Social is the model entity for the Social schema.
type Social struct {
	config `json:"-"`
	// ID of the ent.
	ID int `json:"id,omitempty"`
	// CreatedAt holds the value of the "created_at" field.
	CreatedAt time.Time `json:"created_at,omitempty"`
	// Name holds the value of the "name" field.
	Name string `json:"name,omitempty"`
	// URL holds the value of the "url" field.
	URL string `json:"url,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the SocialQuery when eager-loading is set.
	Edges                    SocialEdges `json:"edges"`
	business_business_social *int
	selectValues             sql.SelectValues
}

// SocialEdges holds the relations/edges for other nodes in the graph.
type SocialEdges struct {
	// Social holds the value of the social edge.
	Social *Business `json:"social,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [1]bool
}

// SocialOrErr returns the Social value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e SocialEdges) SocialOrErr() (*Business, error) {
	if e.Social != nil {
		return e.Social, nil
	} else if e.loadedTypes[0] {
		return nil, &NotFoundError{label: business.Label}
	}
	return nil, &NotLoadedError{edge: "social"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*Social) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case social.FieldID:
			values[i] = new(sql.NullInt64)
		case social.FieldName, social.FieldURL:
			values[i] = new(sql.NullString)
		case social.FieldCreatedAt:
			values[i] = new(sql.NullTime)
		case social.ForeignKeys[0]: // business_business_social
			values[i] = new(sql.NullInt64)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the Social fields.
func (s *Social) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case social.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			s.ID = int(value.Int64)
		case social.FieldCreatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field created_at", values[i])
			} else if value.Valid {
				s.CreatedAt = value.Time
			}
		case social.FieldName:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field name", values[i])
			} else if value.Valid {
				s.Name = value.String
			}
		case social.FieldURL:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field url", values[i])
			} else if value.Valid {
				s.URL = value.String
			}
		case social.ForeignKeys[0]:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for edge-field business_business_social", value)
			} else if value.Valid {
				s.business_business_social = new(int)
				*s.business_business_social = int(value.Int64)
			}
		default:
			s.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the Social.
// This includes values selected through modifiers, order, etc.
func (s *Social) Value(name string) (ent.Value, error) {
	return s.selectValues.Get(name)
}

// QuerySocial queries the "social" edge of the Social entity.
func (s *Social) QuerySocial() *BusinessQuery {
	return NewSocialClient(s.config).QuerySocial(s)
}

// Update returns a builder for updating this Social.
// Note that you need to call Social.Unwrap() before calling this method if this Social
// was returned from a transaction, and the transaction was committed or rolled back.
func (s *Social) Update() *SocialUpdateOne {
	return NewSocialClient(s.config).UpdateOne(s)
}

// Unwrap unwraps the Social entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (s *Social) Unwrap() *Social {
	_tx, ok := s.config.driver.(*txDriver)
	if !ok {
		panic("ent: Social is not a transactional entity")
	}
	s.config.driver = _tx.drv
	return s
}

// String implements the fmt.Stringer.
func (s *Social) String() string {
	var builder strings.Builder
	builder.WriteString("Social(")
	builder.WriteString(fmt.Sprintf("id=%v, ", s.ID))
	builder.WriteString("created_at=")
	builder.WriteString(s.CreatedAt.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("name=")
	builder.WriteString(s.Name)
	builder.WriteString(", ")
	builder.WriteString("url=")
	builder.WriteString(s.URL)
	builder.WriteByte(')')
	return builder.String()
}

// Socials is a parsable slice of Social.
type Socials []*Social
