// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"database/sql/driver"
	"fmt"
	"math"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/Toflex/directory_v2/ent/business"
	"github.com/Toflex/directory_v2/ent/predicate"
	"github.com/Toflex/directory_v2/ent/requestverification"
	"github.com/Toflex/directory_v2/ent/user"
)

// RequestVerificationQuery is the builder for querying RequestVerification entities.
type RequestVerificationQuery struct {
	config
	ctx          *QueryContext
	order        []requestverification.OrderOption
	inters       []Interceptor
	predicates   []predicate.RequestVerification
	withUser     *UserQuery
	withBusiness *BusinessQuery
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the RequestVerificationQuery builder.
func (rvq *RequestVerificationQuery) Where(ps ...predicate.RequestVerification) *RequestVerificationQuery {
	rvq.predicates = append(rvq.predicates, ps...)
	return rvq
}

// Limit the number of records to be returned by this query.
func (rvq *RequestVerificationQuery) Limit(limit int) *RequestVerificationQuery {
	rvq.ctx.Limit = &limit
	return rvq
}

// Offset to start from.
func (rvq *RequestVerificationQuery) Offset(offset int) *RequestVerificationQuery {
	rvq.ctx.Offset = &offset
	return rvq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (rvq *RequestVerificationQuery) Unique(unique bool) *RequestVerificationQuery {
	rvq.ctx.Unique = &unique
	return rvq
}

// Order specifies how the records should be ordered.
func (rvq *RequestVerificationQuery) Order(o ...requestverification.OrderOption) *RequestVerificationQuery {
	rvq.order = append(rvq.order, o...)
	return rvq
}

// QueryUser chains the current query on the "user" edge.
func (rvq *RequestVerificationQuery) QueryUser() *UserQuery {
	query := (&UserClient{config: rvq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := rvq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := rvq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(requestverification.Table, requestverification.FieldID, selector),
			sqlgraph.To(user.Table, user.FieldID),
			sqlgraph.Edge(sqlgraph.M2M, false, requestverification.UserTable, requestverification.UserPrimaryKey...),
		)
		fromU = sqlgraph.SetNeighbors(rvq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryBusiness chains the current query on the "business" edge.
func (rvq *RequestVerificationQuery) QueryBusiness() *BusinessQuery {
	query := (&BusinessClient{config: rvq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := rvq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := rvq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(requestverification.Table, requestverification.FieldID, selector),
			sqlgraph.To(business.Table, business.FieldID),
			sqlgraph.Edge(sqlgraph.M2M, false, requestverification.BusinessTable, requestverification.BusinessPrimaryKey...),
		)
		fromU = sqlgraph.SetNeighbors(rvq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first RequestVerification entity from the query.
// Returns a *NotFoundError when no RequestVerification was found.
func (rvq *RequestVerificationQuery) First(ctx context.Context) (*RequestVerification, error) {
	nodes, err := rvq.Limit(1).All(setContextOp(ctx, rvq.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{requestverification.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (rvq *RequestVerificationQuery) FirstX(ctx context.Context) *RequestVerification {
	node, err := rvq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first RequestVerification ID from the query.
// Returns a *NotFoundError when no RequestVerification ID was found.
func (rvq *RequestVerificationQuery) FirstID(ctx context.Context) (id string, err error) {
	var ids []string
	if ids, err = rvq.Limit(1).IDs(setContextOp(ctx, rvq.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{requestverification.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (rvq *RequestVerificationQuery) FirstIDX(ctx context.Context) string {
	id, err := rvq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single RequestVerification entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one RequestVerification entity is found.
// Returns a *NotFoundError when no RequestVerification entities are found.
func (rvq *RequestVerificationQuery) Only(ctx context.Context) (*RequestVerification, error) {
	nodes, err := rvq.Limit(2).All(setContextOp(ctx, rvq.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{requestverification.Label}
	default:
		return nil, &NotSingularError{requestverification.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (rvq *RequestVerificationQuery) OnlyX(ctx context.Context) *RequestVerification {
	node, err := rvq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only RequestVerification ID in the query.
// Returns a *NotSingularError when more than one RequestVerification ID is found.
// Returns a *NotFoundError when no entities are found.
func (rvq *RequestVerificationQuery) OnlyID(ctx context.Context) (id string, err error) {
	var ids []string
	if ids, err = rvq.Limit(2).IDs(setContextOp(ctx, rvq.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{requestverification.Label}
	default:
		err = &NotSingularError{requestverification.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (rvq *RequestVerificationQuery) OnlyIDX(ctx context.Context) string {
	id, err := rvq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of RequestVerifications.
func (rvq *RequestVerificationQuery) All(ctx context.Context) ([]*RequestVerification, error) {
	ctx = setContextOp(ctx, rvq.ctx, ent.OpQueryAll)
	if err := rvq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*RequestVerification, *RequestVerificationQuery]()
	return withInterceptors[[]*RequestVerification](ctx, rvq, qr, rvq.inters)
}

// AllX is like All, but panics if an error occurs.
func (rvq *RequestVerificationQuery) AllX(ctx context.Context) []*RequestVerification {
	nodes, err := rvq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of RequestVerification IDs.
func (rvq *RequestVerificationQuery) IDs(ctx context.Context) (ids []string, err error) {
	if rvq.ctx.Unique == nil && rvq.path != nil {
		rvq.Unique(true)
	}
	ctx = setContextOp(ctx, rvq.ctx, ent.OpQueryIDs)
	if err = rvq.Select(requestverification.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (rvq *RequestVerificationQuery) IDsX(ctx context.Context) []string {
	ids, err := rvq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (rvq *RequestVerificationQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, rvq.ctx, ent.OpQueryCount)
	if err := rvq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, rvq, querierCount[*RequestVerificationQuery](), rvq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (rvq *RequestVerificationQuery) CountX(ctx context.Context) int {
	count, err := rvq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (rvq *RequestVerificationQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, rvq.ctx, ent.OpQueryExist)
	switch _, err := rvq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (rvq *RequestVerificationQuery) ExistX(ctx context.Context) bool {
	exist, err := rvq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the RequestVerificationQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (rvq *RequestVerificationQuery) Clone() *RequestVerificationQuery {
	if rvq == nil {
		return nil
	}
	return &RequestVerificationQuery{
		config:       rvq.config,
		ctx:          rvq.ctx.Clone(),
		order:        append([]requestverification.OrderOption{}, rvq.order...),
		inters:       append([]Interceptor{}, rvq.inters...),
		predicates:   append([]predicate.RequestVerification{}, rvq.predicates...),
		withUser:     rvq.withUser.Clone(),
		withBusiness: rvq.withBusiness.Clone(),
		// clone intermediate query.
		sql:  rvq.sql.Clone(),
		path: rvq.path,
	}
}

// WithUser tells the query-builder to eager-load the nodes that are connected to
// the "user" edge. The optional arguments are used to configure the query builder of the edge.
func (rvq *RequestVerificationQuery) WithUser(opts ...func(*UserQuery)) *RequestVerificationQuery {
	query := (&UserClient{config: rvq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	rvq.withUser = query
	return rvq
}

// WithBusiness tells the query-builder to eager-load the nodes that are connected to
// the "business" edge. The optional arguments are used to configure the query builder of the edge.
func (rvq *RequestVerificationQuery) WithBusiness(opts ...func(*BusinessQuery)) *RequestVerificationQuery {
	query := (&BusinessClient{config: rvq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	rvq.withBusiness = query
	return rvq
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		CreatedAt time.Time `json:"created_at,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.RequestVerification.Query().
//		GroupBy(requestverification.FieldCreatedAt).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (rvq *RequestVerificationQuery) GroupBy(field string, fields ...string) *RequestVerificationGroupBy {
	rvq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &RequestVerificationGroupBy{build: rvq}
	grbuild.flds = &rvq.ctx.Fields
	grbuild.label = requestverification.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		CreatedAt time.Time `json:"created_at,omitempty"`
//	}
//
//	client.RequestVerification.Query().
//		Select(requestverification.FieldCreatedAt).
//		Scan(ctx, &v)
func (rvq *RequestVerificationQuery) Select(fields ...string) *RequestVerificationSelect {
	rvq.ctx.Fields = append(rvq.ctx.Fields, fields...)
	sbuild := &RequestVerificationSelect{RequestVerificationQuery: rvq}
	sbuild.label = requestverification.Label
	sbuild.flds, sbuild.scan = &rvq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a RequestVerificationSelect configured with the given aggregations.
func (rvq *RequestVerificationQuery) Aggregate(fns ...AggregateFunc) *RequestVerificationSelect {
	return rvq.Select().Aggregate(fns...)
}

func (rvq *RequestVerificationQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range rvq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, rvq); err != nil {
				return err
			}
		}
	}
	for _, f := range rvq.ctx.Fields {
		if !requestverification.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if rvq.path != nil {
		prev, err := rvq.path(ctx)
		if err != nil {
			return err
		}
		rvq.sql = prev
	}
	return nil
}

func (rvq *RequestVerificationQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*RequestVerification, error) {
	var (
		nodes       = []*RequestVerification{}
		_spec       = rvq.querySpec()
		loadedTypes = [2]bool{
			rvq.withUser != nil,
			rvq.withBusiness != nil,
		}
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*RequestVerification).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &RequestVerification{config: rvq.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, rvq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := rvq.withUser; query != nil {
		if err := rvq.loadUser(ctx, query, nodes,
			func(n *RequestVerification) { n.Edges.User = []*User{} },
			func(n *RequestVerification, e *User) { n.Edges.User = append(n.Edges.User, e) }); err != nil {
			return nil, err
		}
	}
	if query := rvq.withBusiness; query != nil {
		if err := rvq.loadBusiness(ctx, query, nodes,
			func(n *RequestVerification) { n.Edges.Business = []*Business{} },
			func(n *RequestVerification, e *Business) { n.Edges.Business = append(n.Edges.Business, e) }); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (rvq *RequestVerificationQuery) loadUser(ctx context.Context, query *UserQuery, nodes []*RequestVerification, init func(*RequestVerification), assign func(*RequestVerification, *User)) error {
	edgeIDs := make([]driver.Value, len(nodes))
	byID := make(map[string]*RequestVerification)
	nids := make(map[string]map[*RequestVerification]struct{})
	for i, node := range nodes {
		edgeIDs[i] = node.ID
		byID[node.ID] = node
		if init != nil {
			init(node)
		}
	}
	query.Where(func(s *sql.Selector) {
		joinT := sql.Table(requestverification.UserTable)
		s.Join(joinT).On(s.C(user.FieldID), joinT.C(requestverification.UserPrimaryKey[1]))
		s.Where(sql.InValues(joinT.C(requestverification.UserPrimaryKey[0]), edgeIDs...))
		columns := s.SelectedColumns()
		s.Select(joinT.C(requestverification.UserPrimaryKey[0]))
		s.AppendSelect(columns...)
		s.SetDistinct(false)
	})
	if err := query.prepareQuery(ctx); err != nil {
		return err
	}
	qr := QuerierFunc(func(ctx context.Context, q Query) (Value, error) {
		return query.sqlAll(ctx, func(_ context.Context, spec *sqlgraph.QuerySpec) {
			assign := spec.Assign
			values := spec.ScanValues
			spec.ScanValues = func(columns []string) ([]any, error) {
				values, err := values(columns[1:])
				if err != nil {
					return nil, err
				}
				return append([]any{new(sql.NullString)}, values...), nil
			}
			spec.Assign = func(columns []string, values []any) error {
				outValue := values[0].(*sql.NullString).String
				inValue := values[1].(*sql.NullString).String
				if nids[inValue] == nil {
					nids[inValue] = map[*RequestVerification]struct{}{byID[outValue]: {}}
					return assign(columns[1:], values[1:])
				}
				nids[inValue][byID[outValue]] = struct{}{}
				return nil
			}
		})
	})
	neighbors, err := withInterceptors[[]*User](ctx, query, qr, query.inters)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected "user" node returned %v`, n.ID)
		}
		for kn := range nodes {
			assign(kn, n)
		}
	}
	return nil
}
func (rvq *RequestVerificationQuery) loadBusiness(ctx context.Context, query *BusinessQuery, nodes []*RequestVerification, init func(*RequestVerification), assign func(*RequestVerification, *Business)) error {
	edgeIDs := make([]driver.Value, len(nodes))
	byID := make(map[string]*RequestVerification)
	nids := make(map[string]map[*RequestVerification]struct{})
	for i, node := range nodes {
		edgeIDs[i] = node.ID
		byID[node.ID] = node
		if init != nil {
			init(node)
		}
	}
	query.Where(func(s *sql.Selector) {
		joinT := sql.Table(requestverification.BusinessTable)
		s.Join(joinT).On(s.C(business.FieldID), joinT.C(requestverification.BusinessPrimaryKey[1]))
		s.Where(sql.InValues(joinT.C(requestverification.BusinessPrimaryKey[0]), edgeIDs...))
		columns := s.SelectedColumns()
		s.Select(joinT.C(requestverification.BusinessPrimaryKey[0]))
		s.AppendSelect(columns...)
		s.SetDistinct(false)
	})
	if err := query.prepareQuery(ctx); err != nil {
		return err
	}
	qr := QuerierFunc(func(ctx context.Context, q Query) (Value, error) {
		return query.sqlAll(ctx, func(_ context.Context, spec *sqlgraph.QuerySpec) {
			assign := spec.Assign
			values := spec.ScanValues
			spec.ScanValues = func(columns []string) ([]any, error) {
				values, err := values(columns[1:])
				if err != nil {
					return nil, err
				}
				return append([]any{new(sql.NullString)}, values...), nil
			}
			spec.Assign = func(columns []string, values []any) error {
				outValue := values[0].(*sql.NullString).String
				inValue := values[1].(*sql.NullString).String
				if nids[inValue] == nil {
					nids[inValue] = map[*RequestVerification]struct{}{byID[outValue]: {}}
					return assign(columns[1:], values[1:])
				}
				nids[inValue][byID[outValue]] = struct{}{}
				return nil
			}
		})
	})
	neighbors, err := withInterceptors[[]*Business](ctx, query, qr, query.inters)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected "business" node returned %v`, n.ID)
		}
		for kn := range nodes {
			assign(kn, n)
		}
	}
	return nil
}

func (rvq *RequestVerificationQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := rvq.querySpec()
	_spec.Node.Columns = rvq.ctx.Fields
	if len(rvq.ctx.Fields) > 0 {
		_spec.Unique = rvq.ctx.Unique != nil && *rvq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, rvq.driver, _spec)
}

func (rvq *RequestVerificationQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(requestverification.Table, requestverification.Columns, sqlgraph.NewFieldSpec(requestverification.FieldID, field.TypeString))
	_spec.From = rvq.sql
	if unique := rvq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if rvq.path != nil {
		_spec.Unique = true
	}
	if fields := rvq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, requestverification.FieldID)
		for i := range fields {
			if fields[i] != requestverification.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := rvq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := rvq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := rvq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := rvq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (rvq *RequestVerificationQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(rvq.driver.Dialect())
	t1 := builder.Table(requestverification.Table)
	columns := rvq.ctx.Fields
	if len(columns) == 0 {
		columns = requestverification.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if rvq.sql != nil {
		selector = rvq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if rvq.ctx.Unique != nil && *rvq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range rvq.predicates {
		p(selector)
	}
	for _, p := range rvq.order {
		p(selector)
	}
	if offset := rvq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := rvq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// RequestVerificationGroupBy is the group-by builder for RequestVerification entities.
type RequestVerificationGroupBy struct {
	selector
	build *RequestVerificationQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (rvgb *RequestVerificationGroupBy) Aggregate(fns ...AggregateFunc) *RequestVerificationGroupBy {
	rvgb.fns = append(rvgb.fns, fns...)
	return rvgb
}

// Scan applies the selector query and scans the result into the given value.
func (rvgb *RequestVerificationGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, rvgb.build.ctx, ent.OpQueryGroupBy)
	if err := rvgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*RequestVerificationQuery, *RequestVerificationGroupBy](ctx, rvgb.build, rvgb, rvgb.build.inters, v)
}

func (rvgb *RequestVerificationGroupBy) sqlScan(ctx context.Context, root *RequestVerificationQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(rvgb.fns))
	for _, fn := range rvgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*rvgb.flds)+len(rvgb.fns))
		for _, f := range *rvgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*rvgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := rvgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// RequestVerificationSelect is the builder for selecting fields of RequestVerification entities.
type RequestVerificationSelect struct {
	*RequestVerificationQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (rvs *RequestVerificationSelect) Aggregate(fns ...AggregateFunc) *RequestVerificationSelect {
	rvs.fns = append(rvs.fns, fns...)
	return rvs
}

// Scan applies the selector query and scans the result into the given value.
func (rvs *RequestVerificationSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, rvs.ctx, ent.OpQuerySelect)
	if err := rvs.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*RequestVerificationQuery, *RequestVerificationSelect](ctx, rvs.RequestVerificationQuery, rvs, rvs.inters, v)
}

func (rvs *RequestVerificationSelect) sqlScan(ctx context.Context, root *RequestVerificationQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(rvs.fns))
	for _, fn := range rvs.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*rvs.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := rvs.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
