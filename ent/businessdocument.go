// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/Toflex/directory_v2/ent/business"
	"github.com/Toflex/directory_v2/ent/businessdocument"
)

// BusinessDocument is the model entity for the BusinessDocument schema.
type BusinessDocument struct {
	config `json:"-"`
	// ID of the ent.
	ID int `json:"id,omitempty"`
	// Title holds the value of the "title" field.
	Title string `json:"title,omitempty"`
	// Description holds the value of the "description" field.
	Description string `json:"description,omitempty"`
	// URL holds the value of the "url" field.
	URL string `json:"url,omitempty"`
	// Verified holds the value of the "verified" field.
	Verified bool `json:"verified,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the BusinessDocumentQuery when eager-loading is set.
	Edges                       BusinessDocumentEdges `json:"edges"`
	business_business_documents *string
	selectValues                sql.SelectValues
}

// BusinessDocumentEdges holds the relations/edges for other nodes in the graph.
type BusinessDocumentEdges struct {
	// BusinessDocument holds the value of the business_document edge.
	BusinessDocument *Business `json:"business_document,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [1]bool
}

// BusinessDocumentOrErr returns the BusinessDocument value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e BusinessDocumentEdges) BusinessDocumentOrErr() (*Business, error) {
	if e.BusinessDocument != nil {
		return e.BusinessDocument, nil
	} else if e.loadedTypes[0] {
		return nil, &NotFoundError{label: business.Label}
	}
	return nil, &NotLoadedError{edge: "business_document"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*BusinessDocument) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case businessdocument.FieldVerified:
			values[i] = new(sql.NullBool)
		case businessdocument.FieldID:
			values[i] = new(sql.NullInt64)
		case businessdocument.FieldTitle, businessdocument.FieldDescription, businessdocument.FieldURL:
			values[i] = new(sql.NullString)
		case businessdocument.ForeignKeys[0]: // business_business_documents
			values[i] = new(sql.NullString)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the BusinessDocument fields.
func (bd *BusinessDocument) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case businessdocument.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			bd.ID = int(value.Int64)
		case businessdocument.FieldTitle:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field title", values[i])
			} else if value.Valid {
				bd.Title = value.String
			}
		case businessdocument.FieldDescription:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field description", values[i])
			} else if value.Valid {
				bd.Description = value.String
			}
		case businessdocument.FieldURL:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field url", values[i])
			} else if value.Valid {
				bd.URL = value.String
			}
		case businessdocument.FieldVerified:
			if value, ok := values[i].(*sql.NullBool); !ok {
				return fmt.Errorf("unexpected type %T for field verified", values[i])
			} else if value.Valid {
				bd.Verified = value.Bool
			}
		case businessdocument.ForeignKeys[0]:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field business_business_documents", values[i])
			} else if value.Valid {
				bd.business_business_documents = new(string)
				*bd.business_business_documents = value.String
			}
		default:
			bd.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the BusinessDocument.
// This includes values selected through modifiers, order, etc.
func (bd *BusinessDocument) Value(name string) (ent.Value, error) {
	return bd.selectValues.Get(name)
}

// QueryBusinessDocument queries the "business_document" edge of the BusinessDocument entity.
func (bd *BusinessDocument) QueryBusinessDocument() *BusinessQuery {
	return NewBusinessDocumentClient(bd.config).QueryBusinessDocument(bd)
}

// Update returns a builder for updating this BusinessDocument.
// Note that you need to call BusinessDocument.Unwrap() before calling this method if this BusinessDocument
// was returned from a transaction, and the transaction was committed or rolled back.
func (bd *BusinessDocument) Update() *BusinessDocumentUpdateOne {
	return NewBusinessDocumentClient(bd.config).UpdateOne(bd)
}

// Unwrap unwraps the BusinessDocument entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (bd *BusinessDocument) Unwrap() *BusinessDocument {
	_tx, ok := bd.config.driver.(*txDriver)
	if !ok {
		panic("ent: BusinessDocument is not a transactional entity")
	}
	bd.config.driver = _tx.drv
	return bd
}

// String implements the fmt.Stringer.
func (bd *BusinessDocument) String() string {
	var builder strings.Builder
	builder.WriteString("BusinessDocument(")
	builder.WriteString(fmt.Sprintf("id=%v, ", bd.ID))
	builder.WriteString("title=")
	builder.WriteString(bd.Title)
	builder.WriteString(", ")
	builder.WriteString("description=")
	builder.WriteString(bd.Description)
	builder.WriteString(", ")
	builder.WriteString("url=")
	builder.WriteString(bd.URL)
	builder.WriteString(", ")
	builder.WriteString("verified=")
	builder.WriteString(fmt.Sprintf("%v", bd.Verified))
	builder.WriteByte(')')
	return builder.String()
}

// BusinessDocuments is a parsable slice of BusinessDocument.
type BusinessDocuments []*BusinessDocument
