// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/Toflex/directory_v2/ent/kybdocument"
)

// KYBDocument is the model entity for the KYBDocument schema.
type KYBDocument struct {
	config `json:"-"`
	// ID of the ent.
	ID string `json:"id,omitempty"`
	// CreatedAt holds the value of the "created_at" field.
	CreatedAt time.Time `json:"created_at,omitempty"`
	// UpdatedAt holds the value of the "updated_at" field.
	UpdatedAt time.Time `json:"updated_at,omitempty"`
	// DeletedAt holds the value of the "deleted_at" field.
	DeletedAt *time.Time `json:"deleted_at,omitempty"`
	// Name holds the value of the "name" field.
	Name string `json:"name,omitempty"`
	// Required holds the value of the "required" field.
	Required bool `json:"required,omitempty"`
	// Active holds the value of the "active" field.
	Active bool `json:"active,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the KYBDocumentQuery when eager-loading is set.
	Edges        KYBDocumentEdges `json:"edges"`
	selectValues sql.SelectValues
}

// KYBDocumentEdges holds the relations/edges for other nodes in the graph.
type KYBDocumentEdges struct {
	// KybDocuments holds the value of the kyb_documents edge.
	KybDocuments []*BusinessDocument `json:"kyb_documents,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [1]bool
}

// KybDocumentsOrErr returns the KybDocuments value or an error if the edge
// was not loaded in eager-loading.
func (e KYBDocumentEdges) KybDocumentsOrErr() ([]*BusinessDocument, error) {
	if e.loadedTypes[0] {
		return e.KybDocuments, nil
	}
	return nil, &NotLoadedError{edge: "kyb_documents"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*KYBDocument) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case kybdocument.FieldRequired, kybdocument.FieldActive:
			values[i] = new(sql.NullBool)
		case kybdocument.FieldID, kybdocument.FieldName:
			values[i] = new(sql.NullString)
		case kybdocument.FieldCreatedAt, kybdocument.FieldUpdatedAt, kybdocument.FieldDeletedAt:
			values[i] = new(sql.NullTime)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the KYBDocument fields.
func (kd *KYBDocument) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case kybdocument.FieldID:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field id", values[i])
			} else if value.Valid {
				kd.ID = value.String
			}
		case kybdocument.FieldCreatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field created_at", values[i])
			} else if value.Valid {
				kd.CreatedAt = value.Time
			}
		case kybdocument.FieldUpdatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field updated_at", values[i])
			} else if value.Valid {
				kd.UpdatedAt = value.Time
			}
		case kybdocument.FieldDeletedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field deleted_at", values[i])
			} else if value.Valid {
				kd.DeletedAt = new(time.Time)
				*kd.DeletedAt = value.Time
			}
		case kybdocument.FieldName:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field name", values[i])
			} else if value.Valid {
				kd.Name = value.String
			}
		case kybdocument.FieldRequired:
			if value, ok := values[i].(*sql.NullBool); !ok {
				return fmt.Errorf("unexpected type %T for field required", values[i])
			} else if value.Valid {
				kd.Required = value.Bool
			}
		case kybdocument.FieldActive:
			if value, ok := values[i].(*sql.NullBool); !ok {
				return fmt.Errorf("unexpected type %T for field active", values[i])
			} else if value.Valid {
				kd.Active = value.Bool
			}
		default:
			kd.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the KYBDocument.
// This includes values selected through modifiers, order, etc.
func (kd *KYBDocument) Value(name string) (ent.Value, error) {
	return kd.selectValues.Get(name)
}

// QueryKybDocuments queries the "kyb_documents" edge of the KYBDocument entity.
func (kd *KYBDocument) QueryKybDocuments() *BusinessDocumentQuery {
	return NewKYBDocumentClient(kd.config).QueryKybDocuments(kd)
}

// Update returns a builder for updating this KYBDocument.
// Note that you need to call KYBDocument.Unwrap() before calling this method if this KYBDocument
// was returned from a transaction, and the transaction was committed or rolled back.
func (kd *KYBDocument) Update() *KYBDocumentUpdateOne {
	return NewKYBDocumentClient(kd.config).UpdateOne(kd)
}

// Unwrap unwraps the KYBDocument entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (kd *KYBDocument) Unwrap() *KYBDocument {
	_tx, ok := kd.config.driver.(*txDriver)
	if !ok {
		panic("ent: KYBDocument is not a transactional entity")
	}
	kd.config.driver = _tx.drv
	return kd
}

// String implements the fmt.Stringer.
func (kd *KYBDocument) String() string {
	var builder strings.Builder
	builder.WriteString("KYBDocument(")
	builder.WriteString(fmt.Sprintf("id=%v, ", kd.ID))
	builder.WriteString("created_at=")
	builder.WriteString(kd.CreatedAt.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("updated_at=")
	builder.WriteString(kd.UpdatedAt.Format(time.ANSIC))
	builder.WriteString(", ")
	if v := kd.DeletedAt; v != nil {
		builder.WriteString("deleted_at=")
		builder.WriteString(v.Format(time.ANSIC))
	}
	builder.WriteString(", ")
	builder.WriteString("name=")
	builder.WriteString(kd.Name)
	builder.WriteString(", ")
	builder.WriteString("required=")
	builder.WriteString(fmt.Sprintf("%v", kd.Required))
	builder.WriteString(", ")
	builder.WriteString("active=")
	builder.WriteString(fmt.Sprintf("%v", kd.Active))
	builder.WriteByte(')')
	return builder.String()
}

// KYBDocuments is a parsable slice of KYBDocument.
type KYBDocuments []*KYBDocument
