// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/Toflex/directory_v2/ent/nubanstaticaccount"
	"github.com/Toflex/directory_v2/ent/predicate"
	"github.com/Toflex/directory_v2/ent/wallet"
)

// NubanStaticAccountQuery is the builder for querying NubanStaticAccount entities.
type NubanStaticAccountQuery struct {
	config
	ctx        *QueryContext
	order      []nubanstaticaccount.OrderOption
	inters     []Interceptor
	predicates []predicate.NubanStaticAccount
	withWallet *WalletQuery
	withFKs    bool
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the NubanStaticAccountQuery builder.
func (nsaq *NubanStaticAccountQuery) Where(ps ...predicate.NubanStaticAccount) *NubanStaticAccountQuery {
	nsaq.predicates = append(nsaq.predicates, ps...)
	return nsaq
}

// Limit the number of records to be returned by this query.
func (nsaq *NubanStaticAccountQuery) Limit(limit int) *NubanStaticAccountQuery {
	nsaq.ctx.Limit = &limit
	return nsaq
}

// Offset to start from.
func (nsaq *NubanStaticAccountQuery) Offset(offset int) *NubanStaticAccountQuery {
	nsaq.ctx.Offset = &offset
	return nsaq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (nsaq *NubanStaticAccountQuery) Unique(unique bool) *NubanStaticAccountQuery {
	nsaq.ctx.Unique = &unique
	return nsaq
}

// Order specifies how the records should be ordered.
func (nsaq *NubanStaticAccountQuery) Order(o ...nubanstaticaccount.OrderOption) *NubanStaticAccountQuery {
	nsaq.order = append(nsaq.order, o...)
	return nsaq
}

// QueryWallet chains the current query on the "wallet" edge.
func (nsaq *NubanStaticAccountQuery) QueryWallet() *WalletQuery {
	query := (&WalletClient{config: nsaq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := nsaq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := nsaq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(nubanstaticaccount.Table, nubanstaticaccount.FieldID, selector),
			sqlgraph.To(wallet.Table, wallet.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, nubanstaticaccount.WalletTable, nubanstaticaccount.WalletColumn),
		)
		fromU = sqlgraph.SetNeighbors(nsaq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first NubanStaticAccount entity from the query.
// Returns a *NotFoundError when no NubanStaticAccount was found.
func (nsaq *NubanStaticAccountQuery) First(ctx context.Context) (*NubanStaticAccount, error) {
	nodes, err := nsaq.Limit(1).All(setContextOp(ctx, nsaq.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{nubanstaticaccount.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (nsaq *NubanStaticAccountQuery) FirstX(ctx context.Context) *NubanStaticAccount {
	node, err := nsaq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first NubanStaticAccount ID from the query.
// Returns a *NotFoundError when no NubanStaticAccount ID was found.
func (nsaq *NubanStaticAccountQuery) FirstID(ctx context.Context) (id string, err error) {
	var ids []string
	if ids, err = nsaq.Limit(1).IDs(setContextOp(ctx, nsaq.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{nubanstaticaccount.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (nsaq *NubanStaticAccountQuery) FirstIDX(ctx context.Context) string {
	id, err := nsaq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single NubanStaticAccount entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one NubanStaticAccount entity is found.
// Returns a *NotFoundError when no NubanStaticAccount entities are found.
func (nsaq *NubanStaticAccountQuery) Only(ctx context.Context) (*NubanStaticAccount, error) {
	nodes, err := nsaq.Limit(2).All(setContextOp(ctx, nsaq.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{nubanstaticaccount.Label}
	default:
		return nil, &NotSingularError{nubanstaticaccount.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (nsaq *NubanStaticAccountQuery) OnlyX(ctx context.Context) *NubanStaticAccount {
	node, err := nsaq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only NubanStaticAccount ID in the query.
// Returns a *NotSingularError when more than one NubanStaticAccount ID is found.
// Returns a *NotFoundError when no entities are found.
func (nsaq *NubanStaticAccountQuery) OnlyID(ctx context.Context) (id string, err error) {
	var ids []string
	if ids, err = nsaq.Limit(2).IDs(setContextOp(ctx, nsaq.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{nubanstaticaccount.Label}
	default:
		err = &NotSingularError{nubanstaticaccount.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (nsaq *NubanStaticAccountQuery) OnlyIDX(ctx context.Context) string {
	id, err := nsaq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of NubanStaticAccounts.
func (nsaq *NubanStaticAccountQuery) All(ctx context.Context) ([]*NubanStaticAccount, error) {
	ctx = setContextOp(ctx, nsaq.ctx, ent.OpQueryAll)
	if err := nsaq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*NubanStaticAccount, *NubanStaticAccountQuery]()
	return withInterceptors[[]*NubanStaticAccount](ctx, nsaq, qr, nsaq.inters)
}

// AllX is like All, but panics if an error occurs.
func (nsaq *NubanStaticAccountQuery) AllX(ctx context.Context) []*NubanStaticAccount {
	nodes, err := nsaq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of NubanStaticAccount IDs.
func (nsaq *NubanStaticAccountQuery) IDs(ctx context.Context) (ids []string, err error) {
	if nsaq.ctx.Unique == nil && nsaq.path != nil {
		nsaq.Unique(true)
	}
	ctx = setContextOp(ctx, nsaq.ctx, ent.OpQueryIDs)
	if err = nsaq.Select(nubanstaticaccount.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (nsaq *NubanStaticAccountQuery) IDsX(ctx context.Context) []string {
	ids, err := nsaq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (nsaq *NubanStaticAccountQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, nsaq.ctx, ent.OpQueryCount)
	if err := nsaq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, nsaq, querierCount[*NubanStaticAccountQuery](), nsaq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (nsaq *NubanStaticAccountQuery) CountX(ctx context.Context) int {
	count, err := nsaq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (nsaq *NubanStaticAccountQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, nsaq.ctx, ent.OpQueryExist)
	switch _, err := nsaq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (nsaq *NubanStaticAccountQuery) ExistX(ctx context.Context) bool {
	exist, err := nsaq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the NubanStaticAccountQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (nsaq *NubanStaticAccountQuery) Clone() *NubanStaticAccountQuery {
	if nsaq == nil {
		return nil
	}
	return &NubanStaticAccountQuery{
		config:     nsaq.config,
		ctx:        nsaq.ctx.Clone(),
		order:      append([]nubanstaticaccount.OrderOption{}, nsaq.order...),
		inters:     append([]Interceptor{}, nsaq.inters...),
		predicates: append([]predicate.NubanStaticAccount{}, nsaq.predicates...),
		withWallet: nsaq.withWallet.Clone(),
		// clone intermediate query.
		sql:  nsaq.sql.Clone(),
		path: nsaq.path,
	}
}

// WithWallet tells the query-builder to eager-load the nodes that are connected to
// the "wallet" edge. The optional arguments are used to configure the query builder of the edge.
func (nsaq *NubanStaticAccountQuery) WithWallet(opts ...func(*WalletQuery)) *NubanStaticAccountQuery {
	query := (&WalletClient{config: nsaq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	nsaq.withWallet = query
	return nsaq
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		CreatedAt time.Time `json:"created_at,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.NubanStaticAccount.Query().
//		GroupBy(nubanstaticaccount.FieldCreatedAt).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (nsaq *NubanStaticAccountQuery) GroupBy(field string, fields ...string) *NubanStaticAccountGroupBy {
	nsaq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &NubanStaticAccountGroupBy{build: nsaq}
	grbuild.flds = &nsaq.ctx.Fields
	grbuild.label = nubanstaticaccount.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		CreatedAt time.Time `json:"created_at,omitempty"`
//	}
//
//	client.NubanStaticAccount.Query().
//		Select(nubanstaticaccount.FieldCreatedAt).
//		Scan(ctx, &v)
func (nsaq *NubanStaticAccountQuery) Select(fields ...string) *NubanStaticAccountSelect {
	nsaq.ctx.Fields = append(nsaq.ctx.Fields, fields...)
	sbuild := &NubanStaticAccountSelect{NubanStaticAccountQuery: nsaq}
	sbuild.label = nubanstaticaccount.Label
	sbuild.flds, sbuild.scan = &nsaq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a NubanStaticAccountSelect configured with the given aggregations.
func (nsaq *NubanStaticAccountQuery) Aggregate(fns ...AggregateFunc) *NubanStaticAccountSelect {
	return nsaq.Select().Aggregate(fns...)
}

func (nsaq *NubanStaticAccountQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range nsaq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, nsaq); err != nil {
				return err
			}
		}
	}
	for _, f := range nsaq.ctx.Fields {
		if !nubanstaticaccount.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if nsaq.path != nil {
		prev, err := nsaq.path(ctx)
		if err != nil {
			return err
		}
		nsaq.sql = prev
	}
	return nil
}

func (nsaq *NubanStaticAccountQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*NubanStaticAccount, error) {
	var (
		nodes       = []*NubanStaticAccount{}
		withFKs     = nsaq.withFKs
		_spec       = nsaq.querySpec()
		loadedTypes = [1]bool{
			nsaq.withWallet != nil,
		}
	)
	if nsaq.withWallet != nil {
		withFKs = true
	}
	if withFKs {
		_spec.Node.Columns = append(_spec.Node.Columns, nubanstaticaccount.ForeignKeys...)
	}
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*NubanStaticAccount).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &NubanStaticAccount{config: nsaq.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, nsaq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := nsaq.withWallet; query != nil {
		if err := nsaq.loadWallet(ctx, query, nodes, nil,
			func(n *NubanStaticAccount, e *Wallet) { n.Edges.Wallet = e }); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (nsaq *NubanStaticAccountQuery) loadWallet(ctx context.Context, query *WalletQuery, nodes []*NubanStaticAccount, init func(*NubanStaticAccount), assign func(*NubanStaticAccount, *Wallet)) error {
	ids := make([]string, 0, len(nodes))
	nodeids := make(map[string][]*NubanStaticAccount)
	for i := range nodes {
		if nodes[i].wallet_nuban_static_account == nil {
			continue
		}
		fk := *nodes[i].wallet_nuban_static_account
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(wallet.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "wallet_nuban_static_account" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}

func (nsaq *NubanStaticAccountQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := nsaq.querySpec()
	_spec.Node.Columns = nsaq.ctx.Fields
	if len(nsaq.ctx.Fields) > 0 {
		_spec.Unique = nsaq.ctx.Unique != nil && *nsaq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, nsaq.driver, _spec)
}

func (nsaq *NubanStaticAccountQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(nubanstaticaccount.Table, nubanstaticaccount.Columns, sqlgraph.NewFieldSpec(nubanstaticaccount.FieldID, field.TypeString))
	_spec.From = nsaq.sql
	if unique := nsaq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if nsaq.path != nil {
		_spec.Unique = true
	}
	if fields := nsaq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, nubanstaticaccount.FieldID)
		for i := range fields {
			if fields[i] != nubanstaticaccount.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := nsaq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := nsaq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := nsaq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := nsaq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (nsaq *NubanStaticAccountQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(nsaq.driver.Dialect())
	t1 := builder.Table(nubanstaticaccount.Table)
	columns := nsaq.ctx.Fields
	if len(columns) == 0 {
		columns = nubanstaticaccount.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if nsaq.sql != nil {
		selector = nsaq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if nsaq.ctx.Unique != nil && *nsaq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range nsaq.predicates {
		p(selector)
	}
	for _, p := range nsaq.order {
		p(selector)
	}
	if offset := nsaq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := nsaq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// NubanStaticAccountGroupBy is the group-by builder for NubanStaticAccount entities.
type NubanStaticAccountGroupBy struct {
	selector
	build *NubanStaticAccountQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (nsagb *NubanStaticAccountGroupBy) Aggregate(fns ...AggregateFunc) *NubanStaticAccountGroupBy {
	nsagb.fns = append(nsagb.fns, fns...)
	return nsagb
}

// Scan applies the selector query and scans the result into the given value.
func (nsagb *NubanStaticAccountGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, nsagb.build.ctx, ent.OpQueryGroupBy)
	if err := nsagb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*NubanStaticAccountQuery, *NubanStaticAccountGroupBy](ctx, nsagb.build, nsagb, nsagb.build.inters, v)
}

func (nsagb *NubanStaticAccountGroupBy) sqlScan(ctx context.Context, root *NubanStaticAccountQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(nsagb.fns))
	for _, fn := range nsagb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*nsagb.flds)+len(nsagb.fns))
		for _, f := range *nsagb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*nsagb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := nsagb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// NubanStaticAccountSelect is the builder for selecting fields of NubanStaticAccount entities.
type NubanStaticAccountSelect struct {
	*NubanStaticAccountQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (nsas *NubanStaticAccountSelect) Aggregate(fns ...AggregateFunc) *NubanStaticAccountSelect {
	nsas.fns = append(nsas.fns, fns...)
	return nsas
}

// Scan applies the selector query and scans the result into the given value.
func (nsas *NubanStaticAccountSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, nsas.ctx, ent.OpQuerySelect)
	if err := nsas.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*NubanStaticAccountQuery, *NubanStaticAccountSelect](ctx, nsas.NubanStaticAccountQuery, nsas, nsas.inters, v)
}

func (nsas *NubanStaticAccountSelect) sqlScan(ctx context.Context, root *NubanStaticAccountQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(nsas.fns))
	for _, fn := range nsas.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*nsas.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := nsas.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
