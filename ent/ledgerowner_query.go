// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"database/sql/driver"
	"fmt"
	"math"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/Toflex/directory_v2/ent/business"
	"github.com/Toflex/directory_v2/ent/ledgerowner"
	"github.com/Toflex/directory_v2/ent/predicate"
	"github.com/Toflex/directory_v2/ent/user"
	"github.com/Toflex/directory_v2/ent/vault"
)

// LedgerOwnerQuery is the builder for querying LedgerOwner entities.
type LedgerOwnerQuery struct {
	config
	ctx          *QueryContext
	order        []ledgerowner.OrderOption
	inters       []Interceptor
	predicates   []predicate.LedgerOwner
	withVaults   *VaultQuery
	withBusiness *BusinessQuery
	withUser     *UserQuery
	withFKs      bool
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the LedgerOwnerQuery builder.
func (loq *LedgerOwnerQuery) Where(ps ...predicate.LedgerOwner) *LedgerOwnerQuery {
	loq.predicates = append(loq.predicates, ps...)
	return loq
}

// Limit the number of records to be returned by this query.
func (loq *LedgerOwnerQuery) Limit(limit int) *LedgerOwnerQuery {
	loq.ctx.Limit = &limit
	return loq
}

// Offset to start from.
func (loq *LedgerOwnerQuery) Offset(offset int) *LedgerOwnerQuery {
	loq.ctx.Offset = &offset
	return loq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (loq *LedgerOwnerQuery) Unique(unique bool) *LedgerOwnerQuery {
	loq.ctx.Unique = &unique
	return loq
}

// Order specifies how the records should be ordered.
func (loq *LedgerOwnerQuery) Order(o ...ledgerowner.OrderOption) *LedgerOwnerQuery {
	loq.order = append(loq.order, o...)
	return loq
}

// QueryVaults chains the current query on the "vaults" edge.
func (loq *LedgerOwnerQuery) QueryVaults() *VaultQuery {
	query := (&VaultClient{config: loq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := loq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := loq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(ledgerowner.Table, ledgerowner.FieldID, selector),
			sqlgraph.To(vault.Table, vault.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, ledgerowner.VaultsTable, ledgerowner.VaultsColumn),
		)
		fromU = sqlgraph.SetNeighbors(loq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryBusiness chains the current query on the "business" edge.
func (loq *LedgerOwnerQuery) QueryBusiness() *BusinessQuery {
	query := (&BusinessClient{config: loq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := loq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := loq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(ledgerowner.Table, ledgerowner.FieldID, selector),
			sqlgraph.To(business.Table, business.FieldID),
			sqlgraph.Edge(sqlgraph.O2O, true, ledgerowner.BusinessTable, ledgerowner.BusinessColumn),
		)
		fromU = sqlgraph.SetNeighbors(loq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryUser chains the current query on the "user" edge.
func (loq *LedgerOwnerQuery) QueryUser() *UserQuery {
	query := (&UserClient{config: loq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := loq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := loq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(ledgerowner.Table, ledgerowner.FieldID, selector),
			sqlgraph.To(user.Table, user.FieldID),
			sqlgraph.Edge(sqlgraph.O2O, true, ledgerowner.UserTable, ledgerowner.UserColumn),
		)
		fromU = sqlgraph.SetNeighbors(loq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first LedgerOwner entity from the query.
// Returns a *NotFoundError when no LedgerOwner was found.
func (loq *LedgerOwnerQuery) First(ctx context.Context) (*LedgerOwner, error) {
	nodes, err := loq.Limit(1).All(setContextOp(ctx, loq.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{ledgerowner.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (loq *LedgerOwnerQuery) FirstX(ctx context.Context) *LedgerOwner {
	node, err := loq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first LedgerOwner ID from the query.
// Returns a *NotFoundError when no LedgerOwner ID was found.
func (loq *LedgerOwnerQuery) FirstID(ctx context.Context) (id string, err error) {
	var ids []string
	if ids, err = loq.Limit(1).IDs(setContextOp(ctx, loq.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{ledgerowner.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (loq *LedgerOwnerQuery) FirstIDX(ctx context.Context) string {
	id, err := loq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single LedgerOwner entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one LedgerOwner entity is found.
// Returns a *NotFoundError when no LedgerOwner entities are found.
func (loq *LedgerOwnerQuery) Only(ctx context.Context) (*LedgerOwner, error) {
	nodes, err := loq.Limit(2).All(setContextOp(ctx, loq.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{ledgerowner.Label}
	default:
		return nil, &NotSingularError{ledgerowner.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (loq *LedgerOwnerQuery) OnlyX(ctx context.Context) *LedgerOwner {
	node, err := loq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only LedgerOwner ID in the query.
// Returns a *NotSingularError when more than one LedgerOwner ID is found.
// Returns a *NotFoundError when no entities are found.
func (loq *LedgerOwnerQuery) OnlyID(ctx context.Context) (id string, err error) {
	var ids []string
	if ids, err = loq.Limit(2).IDs(setContextOp(ctx, loq.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{ledgerowner.Label}
	default:
		err = &NotSingularError{ledgerowner.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (loq *LedgerOwnerQuery) OnlyIDX(ctx context.Context) string {
	id, err := loq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of LedgerOwners.
func (loq *LedgerOwnerQuery) All(ctx context.Context) ([]*LedgerOwner, error) {
	ctx = setContextOp(ctx, loq.ctx, ent.OpQueryAll)
	if err := loq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*LedgerOwner, *LedgerOwnerQuery]()
	return withInterceptors[[]*LedgerOwner](ctx, loq, qr, loq.inters)
}

// AllX is like All, but panics if an error occurs.
func (loq *LedgerOwnerQuery) AllX(ctx context.Context) []*LedgerOwner {
	nodes, err := loq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of LedgerOwner IDs.
func (loq *LedgerOwnerQuery) IDs(ctx context.Context) (ids []string, err error) {
	if loq.ctx.Unique == nil && loq.path != nil {
		loq.Unique(true)
	}
	ctx = setContextOp(ctx, loq.ctx, ent.OpQueryIDs)
	if err = loq.Select(ledgerowner.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (loq *LedgerOwnerQuery) IDsX(ctx context.Context) []string {
	ids, err := loq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (loq *LedgerOwnerQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, loq.ctx, ent.OpQueryCount)
	if err := loq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, loq, querierCount[*LedgerOwnerQuery](), loq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (loq *LedgerOwnerQuery) CountX(ctx context.Context) int {
	count, err := loq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (loq *LedgerOwnerQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, loq.ctx, ent.OpQueryExist)
	switch _, err := loq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (loq *LedgerOwnerQuery) ExistX(ctx context.Context) bool {
	exist, err := loq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the LedgerOwnerQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (loq *LedgerOwnerQuery) Clone() *LedgerOwnerQuery {
	if loq == nil {
		return nil
	}
	return &LedgerOwnerQuery{
		config:       loq.config,
		ctx:          loq.ctx.Clone(),
		order:        append([]ledgerowner.OrderOption{}, loq.order...),
		inters:       append([]Interceptor{}, loq.inters...),
		predicates:   append([]predicate.LedgerOwner{}, loq.predicates...),
		withVaults:   loq.withVaults.Clone(),
		withBusiness: loq.withBusiness.Clone(),
		withUser:     loq.withUser.Clone(),
		// clone intermediate query.
		sql:  loq.sql.Clone(),
		path: loq.path,
	}
}

// WithVaults tells the query-builder to eager-load the nodes that are connected to
// the "vaults" edge. The optional arguments are used to configure the query builder of the edge.
func (loq *LedgerOwnerQuery) WithVaults(opts ...func(*VaultQuery)) *LedgerOwnerQuery {
	query := (&VaultClient{config: loq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	loq.withVaults = query
	return loq
}

// WithBusiness tells the query-builder to eager-load the nodes that are connected to
// the "business" edge. The optional arguments are used to configure the query builder of the edge.
func (loq *LedgerOwnerQuery) WithBusiness(opts ...func(*BusinessQuery)) *LedgerOwnerQuery {
	query := (&BusinessClient{config: loq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	loq.withBusiness = query
	return loq
}

// WithUser tells the query-builder to eager-load the nodes that are connected to
// the "user" edge. The optional arguments are used to configure the query builder of the edge.
func (loq *LedgerOwnerQuery) WithUser(opts ...func(*UserQuery)) *LedgerOwnerQuery {
	query := (&UserClient{config: loq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	loq.withUser = query
	return loq
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		CreatedAt time.Time `json:"created_at,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.LedgerOwner.Query().
//		GroupBy(ledgerowner.FieldCreatedAt).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (loq *LedgerOwnerQuery) GroupBy(field string, fields ...string) *LedgerOwnerGroupBy {
	loq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &LedgerOwnerGroupBy{build: loq}
	grbuild.flds = &loq.ctx.Fields
	grbuild.label = ledgerowner.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		CreatedAt time.Time `json:"created_at,omitempty"`
//	}
//
//	client.LedgerOwner.Query().
//		Select(ledgerowner.FieldCreatedAt).
//		Scan(ctx, &v)
func (loq *LedgerOwnerQuery) Select(fields ...string) *LedgerOwnerSelect {
	loq.ctx.Fields = append(loq.ctx.Fields, fields...)
	sbuild := &LedgerOwnerSelect{LedgerOwnerQuery: loq}
	sbuild.label = ledgerowner.Label
	sbuild.flds, sbuild.scan = &loq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a LedgerOwnerSelect configured with the given aggregations.
func (loq *LedgerOwnerQuery) Aggregate(fns ...AggregateFunc) *LedgerOwnerSelect {
	return loq.Select().Aggregate(fns...)
}

func (loq *LedgerOwnerQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range loq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, loq); err != nil {
				return err
			}
		}
	}
	for _, f := range loq.ctx.Fields {
		if !ledgerowner.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if loq.path != nil {
		prev, err := loq.path(ctx)
		if err != nil {
			return err
		}
		loq.sql = prev
	}
	return nil
}

func (loq *LedgerOwnerQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*LedgerOwner, error) {
	var (
		nodes       = []*LedgerOwner{}
		withFKs     = loq.withFKs
		_spec       = loq.querySpec()
		loadedTypes = [3]bool{
			loq.withVaults != nil,
			loq.withBusiness != nil,
			loq.withUser != nil,
		}
	)
	if loq.withBusiness != nil || loq.withUser != nil {
		withFKs = true
	}
	if withFKs {
		_spec.Node.Columns = append(_spec.Node.Columns, ledgerowner.ForeignKeys...)
	}
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*LedgerOwner).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &LedgerOwner{config: loq.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, loq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := loq.withVaults; query != nil {
		if err := loq.loadVaults(ctx, query, nodes,
			func(n *LedgerOwner) { n.Edges.Vaults = []*Vault{} },
			func(n *LedgerOwner, e *Vault) { n.Edges.Vaults = append(n.Edges.Vaults, e) }); err != nil {
			return nil, err
		}
	}
	if query := loq.withBusiness; query != nil {
		if err := loq.loadBusiness(ctx, query, nodes, nil,
			func(n *LedgerOwner, e *Business) { n.Edges.Business = e }); err != nil {
			return nil, err
		}
	}
	if query := loq.withUser; query != nil {
		if err := loq.loadUser(ctx, query, nodes, nil,
			func(n *LedgerOwner, e *User) { n.Edges.User = e }); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (loq *LedgerOwnerQuery) loadVaults(ctx context.Context, query *VaultQuery, nodes []*LedgerOwner, init func(*LedgerOwner), assign func(*LedgerOwner, *Vault)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[string]*LedgerOwner)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
		if init != nil {
			init(nodes[i])
		}
	}
	query.withFKs = true
	query.Where(predicate.Vault(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(ledgerowner.VaultsColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.ledger_owner_vaults
		if fk == nil {
			return fmt.Errorf(`foreign-key "ledger_owner_vaults" is nil for node %v`, n.ID)
		}
		node, ok := nodeids[*fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "ledger_owner_vaults" returned %v for node %v`, *fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}
func (loq *LedgerOwnerQuery) loadBusiness(ctx context.Context, query *BusinessQuery, nodes []*LedgerOwner, init func(*LedgerOwner), assign func(*LedgerOwner, *Business)) error {
	ids := make([]string, 0, len(nodes))
	nodeids := make(map[string][]*LedgerOwner)
	for i := range nodes {
		if nodes[i].business_owner == nil {
			continue
		}
		fk := *nodes[i].business_owner
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(business.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "business_owner" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}
func (loq *LedgerOwnerQuery) loadUser(ctx context.Context, query *UserQuery, nodes []*LedgerOwner, init func(*LedgerOwner), assign func(*LedgerOwner, *User)) error {
	ids := make([]string, 0, len(nodes))
	nodeids := make(map[string][]*LedgerOwner)
	for i := range nodes {
		if nodes[i].user_owner == nil {
			continue
		}
		fk := *nodes[i].user_owner
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(user.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "user_owner" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}

func (loq *LedgerOwnerQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := loq.querySpec()
	_spec.Node.Columns = loq.ctx.Fields
	if len(loq.ctx.Fields) > 0 {
		_spec.Unique = loq.ctx.Unique != nil && *loq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, loq.driver, _spec)
}

func (loq *LedgerOwnerQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(ledgerowner.Table, ledgerowner.Columns, sqlgraph.NewFieldSpec(ledgerowner.FieldID, field.TypeString))
	_spec.From = loq.sql
	if unique := loq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if loq.path != nil {
		_spec.Unique = true
	}
	if fields := loq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, ledgerowner.FieldID)
		for i := range fields {
			if fields[i] != ledgerowner.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := loq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := loq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := loq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := loq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (loq *LedgerOwnerQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(loq.driver.Dialect())
	t1 := builder.Table(ledgerowner.Table)
	columns := loq.ctx.Fields
	if len(columns) == 0 {
		columns = ledgerowner.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if loq.sql != nil {
		selector = loq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if loq.ctx.Unique != nil && *loq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range loq.predicates {
		p(selector)
	}
	for _, p := range loq.order {
		p(selector)
	}
	if offset := loq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := loq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// LedgerOwnerGroupBy is the group-by builder for LedgerOwner entities.
type LedgerOwnerGroupBy struct {
	selector
	build *LedgerOwnerQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (logb *LedgerOwnerGroupBy) Aggregate(fns ...AggregateFunc) *LedgerOwnerGroupBy {
	logb.fns = append(logb.fns, fns...)
	return logb
}

// Scan applies the selector query and scans the result into the given value.
func (logb *LedgerOwnerGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, logb.build.ctx, ent.OpQueryGroupBy)
	if err := logb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*LedgerOwnerQuery, *LedgerOwnerGroupBy](ctx, logb.build, logb, logb.build.inters, v)
}

func (logb *LedgerOwnerGroupBy) sqlScan(ctx context.Context, root *LedgerOwnerQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(logb.fns))
	for _, fn := range logb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*logb.flds)+len(logb.fns))
		for _, f := range *logb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*logb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := logb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// LedgerOwnerSelect is the builder for selecting fields of LedgerOwner entities.
type LedgerOwnerSelect struct {
	*LedgerOwnerQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (los *LedgerOwnerSelect) Aggregate(fns ...AggregateFunc) *LedgerOwnerSelect {
	los.fns = append(los.fns, fns...)
	return los
}

// Scan applies the selector query and scans the result into the given value.
func (los *LedgerOwnerSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, los.ctx, ent.OpQuerySelect)
	if err := los.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*LedgerOwnerQuery, *LedgerOwnerSelect](ctx, los.LedgerOwnerQuery, los, los.inters, v)
}

func (los *LedgerOwnerSelect) sqlScan(ctx context.Context, root *LedgerOwnerQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(los.fns))
	for _, fn := range los.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*los.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := los.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
