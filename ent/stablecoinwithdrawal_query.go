// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/Toflex/directory_v2/ent/predicate"
	"github.com/Toflex/directory_v2/ent/stablecoinwithdrawal"
	"github.com/Toflex/directory_v2/ent/transaction"
)

// StablecoinWithdrawalQuery is the builder for querying StablecoinWithdrawal entities.
type StablecoinWithdrawalQuery struct {
	config
	ctx             *QueryContext
	order           []stablecoinwithdrawal.OrderOption
	inters          []Interceptor
	predicates      []predicate.StablecoinWithdrawal
	withTransaction *TransactionQuery
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the StablecoinWithdrawalQuery builder.
func (swq *StablecoinWithdrawalQuery) Where(ps ...predicate.StablecoinWithdrawal) *StablecoinWithdrawalQuery {
	swq.predicates = append(swq.predicates, ps...)
	return swq
}

// Limit the number of records to be returned by this query.
func (swq *StablecoinWithdrawalQuery) Limit(limit int) *StablecoinWithdrawalQuery {
	swq.ctx.Limit = &limit
	return swq
}

// Offset to start from.
func (swq *StablecoinWithdrawalQuery) Offset(offset int) *StablecoinWithdrawalQuery {
	swq.ctx.Offset = &offset
	return swq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (swq *StablecoinWithdrawalQuery) Unique(unique bool) *StablecoinWithdrawalQuery {
	swq.ctx.Unique = &unique
	return swq
}

// Order specifies how the records should be ordered.
func (swq *StablecoinWithdrawalQuery) Order(o ...stablecoinwithdrawal.OrderOption) *StablecoinWithdrawalQuery {
	swq.order = append(swq.order, o...)
	return swq
}

// QueryTransaction chains the current query on the "transaction" edge.
func (swq *StablecoinWithdrawalQuery) QueryTransaction() *TransactionQuery {
	query := (&TransactionClient{config: swq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := swq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := swq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(stablecoinwithdrawal.Table, stablecoinwithdrawal.FieldID, selector),
			sqlgraph.To(transaction.Table, transaction.FieldID),
			sqlgraph.Edge(sqlgraph.O2O, true, stablecoinwithdrawal.TransactionTable, stablecoinwithdrawal.TransactionColumn),
		)
		fromU = sqlgraph.SetNeighbors(swq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first StablecoinWithdrawal entity from the query.
// Returns a *NotFoundError when no StablecoinWithdrawal was found.
func (swq *StablecoinWithdrawalQuery) First(ctx context.Context) (*StablecoinWithdrawal, error) {
	nodes, err := swq.Limit(1).All(setContextOp(ctx, swq.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{stablecoinwithdrawal.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (swq *StablecoinWithdrawalQuery) FirstX(ctx context.Context) *StablecoinWithdrawal {
	node, err := swq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first StablecoinWithdrawal ID from the query.
// Returns a *NotFoundError when no StablecoinWithdrawal ID was found.
func (swq *StablecoinWithdrawalQuery) FirstID(ctx context.Context) (id string, err error) {
	var ids []string
	if ids, err = swq.Limit(1).IDs(setContextOp(ctx, swq.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{stablecoinwithdrawal.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (swq *StablecoinWithdrawalQuery) FirstIDX(ctx context.Context) string {
	id, err := swq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single StablecoinWithdrawal entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one StablecoinWithdrawal entity is found.
// Returns a *NotFoundError when no StablecoinWithdrawal entities are found.
func (swq *StablecoinWithdrawalQuery) Only(ctx context.Context) (*StablecoinWithdrawal, error) {
	nodes, err := swq.Limit(2).All(setContextOp(ctx, swq.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{stablecoinwithdrawal.Label}
	default:
		return nil, &NotSingularError{stablecoinwithdrawal.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (swq *StablecoinWithdrawalQuery) OnlyX(ctx context.Context) *StablecoinWithdrawal {
	node, err := swq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only StablecoinWithdrawal ID in the query.
// Returns a *NotSingularError when more than one StablecoinWithdrawal ID is found.
// Returns a *NotFoundError when no entities are found.
func (swq *StablecoinWithdrawalQuery) OnlyID(ctx context.Context) (id string, err error) {
	var ids []string
	if ids, err = swq.Limit(2).IDs(setContextOp(ctx, swq.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{stablecoinwithdrawal.Label}
	default:
		err = &NotSingularError{stablecoinwithdrawal.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (swq *StablecoinWithdrawalQuery) OnlyIDX(ctx context.Context) string {
	id, err := swq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of StablecoinWithdrawals.
func (swq *StablecoinWithdrawalQuery) All(ctx context.Context) ([]*StablecoinWithdrawal, error) {
	ctx = setContextOp(ctx, swq.ctx, ent.OpQueryAll)
	if err := swq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*StablecoinWithdrawal, *StablecoinWithdrawalQuery]()
	return withInterceptors[[]*StablecoinWithdrawal](ctx, swq, qr, swq.inters)
}

// AllX is like All, but panics if an error occurs.
func (swq *StablecoinWithdrawalQuery) AllX(ctx context.Context) []*StablecoinWithdrawal {
	nodes, err := swq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of StablecoinWithdrawal IDs.
func (swq *StablecoinWithdrawalQuery) IDs(ctx context.Context) (ids []string, err error) {
	if swq.ctx.Unique == nil && swq.path != nil {
		swq.Unique(true)
	}
	ctx = setContextOp(ctx, swq.ctx, ent.OpQueryIDs)
	if err = swq.Select(stablecoinwithdrawal.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (swq *StablecoinWithdrawalQuery) IDsX(ctx context.Context) []string {
	ids, err := swq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (swq *StablecoinWithdrawalQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, swq.ctx, ent.OpQueryCount)
	if err := swq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, swq, querierCount[*StablecoinWithdrawalQuery](), swq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (swq *StablecoinWithdrawalQuery) CountX(ctx context.Context) int {
	count, err := swq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (swq *StablecoinWithdrawalQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, swq.ctx, ent.OpQueryExist)
	switch _, err := swq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (swq *StablecoinWithdrawalQuery) ExistX(ctx context.Context) bool {
	exist, err := swq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the StablecoinWithdrawalQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (swq *StablecoinWithdrawalQuery) Clone() *StablecoinWithdrawalQuery {
	if swq == nil {
		return nil
	}
	return &StablecoinWithdrawalQuery{
		config:          swq.config,
		ctx:             swq.ctx.Clone(),
		order:           append([]stablecoinwithdrawal.OrderOption{}, swq.order...),
		inters:          append([]Interceptor{}, swq.inters...),
		predicates:      append([]predicate.StablecoinWithdrawal{}, swq.predicates...),
		withTransaction: swq.withTransaction.Clone(),
		// clone intermediate query.
		sql:  swq.sql.Clone(),
		path: swq.path,
	}
}

// WithTransaction tells the query-builder to eager-load the nodes that are connected to
// the "transaction" edge. The optional arguments are used to configure the query builder of the edge.
func (swq *StablecoinWithdrawalQuery) WithTransaction(opts ...func(*TransactionQuery)) *StablecoinWithdrawalQuery {
	query := (&TransactionClient{config: swq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	swq.withTransaction = query
	return swq
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		CreatedAt time.Time `json:"created_at,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.StablecoinWithdrawal.Query().
//		GroupBy(stablecoinwithdrawal.FieldCreatedAt).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (swq *StablecoinWithdrawalQuery) GroupBy(field string, fields ...string) *StablecoinWithdrawalGroupBy {
	swq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &StablecoinWithdrawalGroupBy{build: swq}
	grbuild.flds = &swq.ctx.Fields
	grbuild.label = stablecoinwithdrawal.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		CreatedAt time.Time `json:"created_at,omitempty"`
//	}
//
//	client.StablecoinWithdrawal.Query().
//		Select(stablecoinwithdrawal.FieldCreatedAt).
//		Scan(ctx, &v)
func (swq *StablecoinWithdrawalQuery) Select(fields ...string) *StablecoinWithdrawalSelect {
	swq.ctx.Fields = append(swq.ctx.Fields, fields...)
	sbuild := &StablecoinWithdrawalSelect{StablecoinWithdrawalQuery: swq}
	sbuild.label = stablecoinwithdrawal.Label
	sbuild.flds, sbuild.scan = &swq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a StablecoinWithdrawalSelect configured with the given aggregations.
func (swq *StablecoinWithdrawalQuery) Aggregate(fns ...AggregateFunc) *StablecoinWithdrawalSelect {
	return swq.Select().Aggregate(fns...)
}

func (swq *StablecoinWithdrawalQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range swq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, swq); err != nil {
				return err
			}
		}
	}
	for _, f := range swq.ctx.Fields {
		if !stablecoinwithdrawal.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if swq.path != nil {
		prev, err := swq.path(ctx)
		if err != nil {
			return err
		}
		swq.sql = prev
	}
	return nil
}

func (swq *StablecoinWithdrawalQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*StablecoinWithdrawal, error) {
	var (
		nodes       = []*StablecoinWithdrawal{}
		_spec       = swq.querySpec()
		loadedTypes = [1]bool{
			swq.withTransaction != nil,
		}
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*StablecoinWithdrawal).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &StablecoinWithdrawal{config: swq.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, swq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := swq.withTransaction; query != nil {
		if err := swq.loadTransaction(ctx, query, nodes, nil,
			func(n *StablecoinWithdrawal, e *Transaction) { n.Edges.Transaction = e }); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (swq *StablecoinWithdrawalQuery) loadTransaction(ctx context.Context, query *TransactionQuery, nodes []*StablecoinWithdrawal, init func(*StablecoinWithdrawal), assign func(*StablecoinWithdrawal, *Transaction)) error {
	ids := make([]string, 0, len(nodes))
	nodeids := make(map[string][]*StablecoinWithdrawal)
	for i := range nodes {
		fk := nodes[i].TransactionID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(transaction.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "transaction_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}

func (swq *StablecoinWithdrawalQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := swq.querySpec()
	_spec.Node.Columns = swq.ctx.Fields
	if len(swq.ctx.Fields) > 0 {
		_spec.Unique = swq.ctx.Unique != nil && *swq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, swq.driver, _spec)
}

func (swq *StablecoinWithdrawalQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(stablecoinwithdrawal.Table, stablecoinwithdrawal.Columns, sqlgraph.NewFieldSpec(stablecoinwithdrawal.FieldID, field.TypeString))
	_spec.From = swq.sql
	if unique := swq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if swq.path != nil {
		_spec.Unique = true
	}
	if fields := swq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, stablecoinwithdrawal.FieldID)
		for i := range fields {
			if fields[i] != stablecoinwithdrawal.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
		if swq.withTransaction != nil {
			_spec.Node.AddColumnOnce(stablecoinwithdrawal.FieldTransactionID)
		}
	}
	if ps := swq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := swq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := swq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := swq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (swq *StablecoinWithdrawalQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(swq.driver.Dialect())
	t1 := builder.Table(stablecoinwithdrawal.Table)
	columns := swq.ctx.Fields
	if len(columns) == 0 {
		columns = stablecoinwithdrawal.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if swq.sql != nil {
		selector = swq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if swq.ctx.Unique != nil && *swq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range swq.predicates {
		p(selector)
	}
	for _, p := range swq.order {
		p(selector)
	}
	if offset := swq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := swq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// StablecoinWithdrawalGroupBy is the group-by builder for StablecoinWithdrawal entities.
type StablecoinWithdrawalGroupBy struct {
	selector
	build *StablecoinWithdrawalQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (swgb *StablecoinWithdrawalGroupBy) Aggregate(fns ...AggregateFunc) *StablecoinWithdrawalGroupBy {
	swgb.fns = append(swgb.fns, fns...)
	return swgb
}

// Scan applies the selector query and scans the result into the given value.
func (swgb *StablecoinWithdrawalGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, swgb.build.ctx, ent.OpQueryGroupBy)
	if err := swgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*StablecoinWithdrawalQuery, *StablecoinWithdrawalGroupBy](ctx, swgb.build, swgb, swgb.build.inters, v)
}

func (swgb *StablecoinWithdrawalGroupBy) sqlScan(ctx context.Context, root *StablecoinWithdrawalQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(swgb.fns))
	for _, fn := range swgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*swgb.flds)+len(swgb.fns))
		for _, f := range *swgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*swgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := swgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// StablecoinWithdrawalSelect is the builder for selecting fields of StablecoinWithdrawal entities.
type StablecoinWithdrawalSelect struct {
	*StablecoinWithdrawalQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (sws *StablecoinWithdrawalSelect) Aggregate(fns ...AggregateFunc) *StablecoinWithdrawalSelect {
	sws.fns = append(sws.fns, fns...)
	return sws
}

// Scan applies the selector query and scans the result into the given value.
func (sws *StablecoinWithdrawalSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, sws.ctx, ent.OpQuerySelect)
	if err := sws.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*StablecoinWithdrawalQuery, *StablecoinWithdrawalSelect](ctx, sws.StablecoinWithdrawalQuery, sws, sws.inters, v)
}

func (sws *StablecoinWithdrawalSelect) sqlScan(ctx context.Context, root *StablecoinWithdrawalQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(sws.fns))
	for _, fn := range sws.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*sws.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := sws.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
